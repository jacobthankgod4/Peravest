Compiled with problems:
×
ERROR in src/components/ajo/ApprovalQueue.tsx:26:64
TS2339: Property 'getGroupJoinRequests' does not exist on type '{ submitJoinRequest(userId: number, groupId: number): Promise<{ data: any; error: any; } | { data: any; error: any; }>; approveJoinRequest(requestId: number, adminId: number): Promise<...>; ... 5 more ...; subscribeToJoinRequests(groupId: number, callback: (request: any) => void): RealtimeChannel; }'.
    24 |       setIsLoading(true);
    25 |       setError(null);
  > 26 |       const { data, error: err } = await ajoJoinRequestService.getGroupJoinRequests(groupId);
       |                                                                ^^^^^^^^^^^^^^^^^^^^
    27 |       if (err) throw err;
    28 |       setRequests(data || []);
    29 |     } catch (err) {
ERROR in src/components/ajo/GroupAjoForm.tsx:2:31
TS2307: Cannot find module '../contexts/OnboardingContext' or its corresponding type declarations.
    1 | import React from 'react';
  > 2 | import { useOnboarding } from '../contexts/OnboardingContext';
      |                               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    3 | import { calculateTotalContribution, formatCurrency, getFrequencyLabel } from '../utils/ajoFormValidation';
    4 |
    5 | export const GroupAjoForm: React.FC = () => {
ERROR in src/components/ajo/GroupAjoForm.tsx:3:79
TS2307: Cannot find module '../utils/ajoFormValidation' or its corresponding type declarations.
    1 | import React from 'react';
    2 | import { useOnboarding } from '../contexts/OnboardingContext';
  > 3 | import { calculateTotalContribution, formatCurrency, getFrequencyLabel } from '../utils/ajoFormValidation';
      |                                                                               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    4 |
    5 | export const GroupAjoForm: React.FC = () => {
    6 |   const { formData, errors, updateFormData, goBack, goNext } = useOnboarding();
ERROR in src/components/ajo/PersonalAjoForm.tsx:2:31
TS2307: Cannot find module '../contexts/OnboardingContext' or its corresponding type declarations.
    1 | import React from 'react';
  > 2 | import { useOnboarding } from '../contexts/OnboardingContext';
      |                               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    3 | import { calculateTotalContribution, formatCurrency, getFrequencyLabel } from '../utils/ajoFormValidation';
    4 |
    5 | export const PersonalAjoForm: React.FC = () => {
ERROR in src/components/ajo/PersonalAjoForm.tsx:3:79
TS2307: Cannot find module '../utils/ajoFormValidation' or its corresponding type declarations.
    1 | import React from 'react';
    2 | import { useOnboarding } from '../contexts/OnboardingContext';
  > 3 | import { calculateTotalContribution, formatCurrency, getFrequencyLabel } from '../utils/ajoFormValidation';
      |                                                                               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    4 |
    5 | export const PersonalAjoForm: React.FC = () => {
    6 |   const { formData, errors, updateFormData, goBack, goNext } = useOnboarding();
ERROR in src/hooks/useAjoRealtime.ts:2:26
TS2307: Cannot find module 'react-query' or its corresponding type declarations.
    1 | import { useEffect, useState, useCallback } from 'react';
  > 2 | import { useQuery } from 'react-query';
      |                          ^^^^^^^^^^^^^
    3 | import { supabase } from '../lib/supabase';
    4 |
    5 | export const useAjoGroups = (userId: number) => {
ERROR in src/hooks/useAjoRealtime.ts:114:8
TS2339: Property 'on' does not exist on type 'SupabaseClient<any, "public", "public", any, any>'.
    112 |   useEffect(() => {
    113 |     const subscription = supabase
  > 114 |       .on('postgres_changes', { event: '*', schema: 'public', table }, callback)
        |        ^^
    115 |       .subscribe();
    116 |
    117 |     return () => {
ERROR in src/pages/AjoAdmin.tsx:2:26
TS2307: Cannot find module 'react-query' or its corresponding type declarations.
    1 | import React, { useState } from 'react';
  > 2 | import { useQuery } from 'react-query';
      |                          ^^^^^^^^^^^^^
    3 | import { supabase } from '../lib/supabase';
    4 | import { UserLayout } from '../components/UserLayout';
    5 | import { LoadingSpinner } from '../components/LoadingSpinner';
ERROR in src/pages/AjoAdmin.tsx:4:10
TS2614: Module '"../components/UserLayout"' has no exported member 'UserLayout'. Did you mean to use 'import UserLayout from "../components/UserLayout"' instead?
    2 | import { useQuery } from 'react-query';
    3 | import { supabase } from '../lib/supabase';
  > 4 | import { UserLayout } from '../components/UserLayout';
      |          ^^^^^^^^^^
    5 | import { LoadingSpinner } from '../components/LoadingSpinner';
    6 | import { Alert } from '../components/Alert';
    7 |
ERROR in src/pages/AjoAdmin.tsx:5:10
TS2614: Module '"../components/LoadingSpinner"' has no exported member 'LoadingSpinner'. Did you mean to use 'import LoadingSpinner from "../components/LoadingSpinner"' instead?
    3 | import { supabase } from '../lib/supabase';
    4 | import { UserLayout } from '../components/UserLayout';
  > 5 | import { LoadingSpinner } from '../components/LoadingSpinner';
      |          ^^^^^^^^^^^^^^
    6 | import { Alert } from '../components/Alert';
    7 |
    8 | export default function AjoAdmin() {
ERROR in src/pages/AjoAdmin.tsx:6:10
TS2614: Module '"../components/Alert"' has no exported member 'Alert'. Did you mean to use 'import Alert from "../components/Alert"' instead?
    4 | import { UserLayout } from '../components/UserLayout';
    5 | import { LoadingSpinner } from '../components/LoadingSpinner';
  > 6 | import { Alert } from '../components/Alert';
      |          ^^^^^
    7 |
    8 | export default function AjoAdmin() {
    9 |   const [activeTab, setActiveTab] = useState<'groups' | 'cycles' | 'members'>('groups');
ERROR in src/pages/AjoCheckout.tsx:7:10
TS2614: Module '"../components/Alert"' has no exported member 'Alert'. Did you mean to use 'import Alert from "../components/Alert"' instead?
     5 | import { ajoPaymentService } from '../services/ajoPaymentService';
     6 | import { formatCurrency, getFrequencyLabel } from '../utils/ajoFormValidation';
  >  7 | import { Alert } from '../components/Alert';
       |          ^^^^^
     8 |
     9 | export const AjoCheckout: React.FC = () => {
    10 |   const navigate = useNavigate();
ERROR in src/pages/AjoCheckout.tsx:12:21
TS2339: Property 'setIsSubmitting' does not exist on type 'OnboardingContextType'.
    10 |   const navigate = useNavigate();
    11 |   const { user } = useAuth();
  > 12 |   const { formData, setIsSubmitting, isSubmitting } = useOnboarding();
       |                     ^^^^^^^^^^^^^^^
    13 |   const [error, setError] = useState('');
    14 |   const [isLoading, setIsLoading] = useState(false);
    15 |
ERROR in src/pages/AjoCheckout.tsx:12:38
TS2339: Property 'isSubmitting' does not exist on type 'OnboardingContextType'.
    10 |   const navigate = useNavigate();
    11 |   const { user } = useAuth();
  > 12 |   const { formData, setIsSubmitting, isSubmitting } = useOnboarding();
       |                                      ^^^^^^^^^^^^
    13 |   const [error, setError] = useState('');
    14 |   const [isLoading, setIsLoading] = useState(false);
    15 |
ERROR in src/pages/AjoCheckout.tsx:31:33
TS2339: Property 'full_name' does not exist on type 'User'.
    29 |     try {
    30 |       // Get user's full name and phone from profile
  > 31 |       const userFullName = user.full_name || user.email.split('@')[0];
       |                                 ^^^^^^^^^
    32 |       const userPhone = user.phone_number || '+234';
    33 |
    34 |       // Initialize payment
ERROR in src/pages/AjoCheckout.tsx:32:30
TS2339: Property 'phone_number' does not exist on type 'User'.
    30 |       // Get user's full name and phone from profile
    31 |       const userFullName = user.full_name || user.email.split('@')[0];
  > 32 |       const userPhone = user.phone_number || '+234';
       |                              ^^^^^^^^^^^^
    33 |
    34 |       // Initialize payment
    35 |       const paymentInit = await ajoPaymentService.initializePayment({
ERROR in src/pages/AjoCheckout.tsx:40:9
TS2322: Type 'string' is not assignable to type 'number'.
    38 |         ajoType: formData.type,
    39 |         ajoData: formData,
  > 40 |         userId: user.id,
       |         ^^^^^^
    41 |         fullName: userFullName,
    42 |         phoneNumber: userPhone,
    43 |       });
ERROR in src/pages/AjoContribute.tsx:6:10
TS2614: Module '"../components/Alert"' has no exported member 'Alert'. Did you mean to use 'import Alert from "../components/Alert"' instead?
    4 | import { contributionPaymentService } from '../services/contributionPaymentService';
    5 | import { formatCurrency, getFrequencyLabel } from '../utils/ajoFormValidation';
  > 6 | import { Alert } from '../components/Alert';
      |          ^^^^^
    7 |
    8 | interface PendingContribution {
    9 |   id: number;
ERROR in src/pages/AjoContribute.tsx:43:86
TS2345: Argument of type 'string' is not assignable to parameter of type 'number'.
    41 |     try {
    42 |       setIsLoading(true);
  > 43 |       const contributions = await contributionPaymentService.getPendingContributions(user.id);
       |                                                                                      ^^^^^^^
    44 |       setPendingContributions(contributions);
    45 |     } catch (err: any) {
    46 |       setError(err.message || 'Failed to fetch pending contributions');
ERROR in src/pages/AjoContribute.tsx:63:9
TS2322: Type 'string' is not assignable to type 'number'.
    61 |     try {
    62 |       const paymentInit = await contributionPaymentService.initializeContributionPayment({
  > 63 |         userId: user.id,
       |         ^^^^^^
    64 |         groupId: contribution.group_id,
    65 |         cycleId: contribution.cycle_id,
    66 |         amount: contribution.amount_due,
ERROR in src/pages/AjoContribute.tsx:68:24
TS2339: Property 'full_name' does not exist on type 'User'.
    66 |         amount: contribution.amount_due,
    67 |         email: user.email,
  > 68 |         fullName: user.full_name || user.email.split('@')[0],
       |                        ^^^^^^^^^
    69 |         phoneNumber: user.phone_number || '+234',
    70 |       });
    71 |
ERROR in src/pages/AjoContribute.tsx:69:27
TS2339: Property 'phone_number' does not exist on type 'User'.
    67 |         email: user.email,
    68 |         fullName: user.full_name || user.email.split('@')[0],
  > 69 |         phoneNumber: user.phone_number || '+234',
       |                           ^^^^^^^^^^^^
    70 |       });
    71 |
    72 |       // Redirect to Flutterwave
ERROR in src/pages/AjoDashboard.tsx:2:26
TS2307: Cannot find module 'react-query' or its corresponding type declarations.
    1 | import React, { useEffect, useState } from 'react';
  > 2 | import { useQuery } from 'react-query';
      |                          ^^^^^^^^^^^^^
    3 | import { supabase } from '../lib/supabase';
    4 | import { ajoGroupService } from '../services/ajoGroupService';
    5 | import { ajoService } from '../services/ajoService';
ERROR in src/pages/AjoDashboard.tsx:6:10
TS2614: Module '"../components/UserLayout"' has no exported member 'UserLayout'. Did you mean to use 'import UserLayout from "../components/UserLayout"' instead?
    4 | import { ajoGroupService } from '../services/ajoGroupService';
    5 | import { ajoService } from '../services/ajoService';
  > 6 | import { UserLayout } from '../components/UserLayout';
      |          ^^^^^^^^^^
    7 | import { LoadingSpinner } from '../components/LoadingSpinner';
    8 | import { Alert } from '../components/Alert';
    9 |
ERROR in src/pages/AjoDashboard.tsx:7:10
TS2614: Module '"../components/LoadingSpinner"' has no exported member 'LoadingSpinner'. Did you mean to use 'import LoadingSpinner from "../components/LoadingSpinner"' instead?
     5 | import { ajoService } from '../services/ajoService';
     6 | import { UserLayout } from '../components/UserLayout';
  >  7 | import { LoadingSpinner } from '../components/LoadingSpinner';
       |          ^^^^^^^^^^^^^^
     8 | import { Alert } from '../components/Alert';
     9 |
    10 | interface DashboardStats {
ERROR in src/pages/AjoDashboard.tsx:8:10
TS2614: Module '"../components/Alert"' has no exported member 'Alert'. Did you mean to use 'import Alert from "../components/Alert"' instead?
     6 | import { UserLayout } from '../components/UserLayout';
     7 | import { LoadingSpinner } from '../components/LoadingSpinner';
  >  8 | import { Alert } from '../components/Alert';
       |          ^^^^^
     9 |
    10 | interface DashboardStats {
    11 |   activeGroups: number;
ERROR in src/pages/AjoDashboardEnhanced.tsx:10:64
TS2345: Argument of type 'string | 0' is not assignable to parameter of type 'number'.
  Type 'string' is not assignable to type 'number'.
     8 |   const [activeTab, setActiveTab] = useState<'overview' | 'groups' | 'transactions' | 'alerts'>('overview');
     9 |   
  > 10 |   const { data: stats, isLoading: statsLoading } = useAjoStats(user?.id || 0);
       |                                                                ^^^^^^^^^^^^^
    11 |   const { data: groups, isLoading: groupsLoading } = useAjoGroups(user?.id || 0);
    12 |   const { data: transactions, isLoading: txLoading } = useAjoTransactions(user?.id || 0);
    13 |   const { data: graceNotices, isLoading: noticesLoading } = useAjoGraceNotices(user?.id || 0);
ERROR in src/pages/AjoDashboardEnhanced.tsx:11:67
TS2345: Argument of type 'string | 0' is not assignable to parameter of type 'number'.
  Type 'string' is not assignable to type 'number'.
     9 |   
    10 |   const { data: stats, isLoading: statsLoading } = useAjoStats(user?.id || 0);
  > 11 |   const { data: groups, isLoading: groupsLoading } = useAjoGroups(user?.id || 0);
       |                                                                   ^^^^^^^^^^^^^
    12 |   const { data: transactions, isLoading: txLoading } = useAjoTransactions(user?.id || 0);
    13 |   const { data: graceNotices, isLoading: noticesLoading } = useAjoGraceNotices(user?.id || 0);
    14 |
ERROR in src/pages/AjoDashboardEnhanced.tsx:12:75
TS2345: Argument of type 'string | 0' is not assignable to parameter of type 'number'.
  Type 'string' is not assignable to type 'number'.
    10 |   const { data: stats, isLoading: statsLoading } = useAjoStats(user?.id || 0);
    11 |   const { data: groups, isLoading: groupsLoading } = useAjoGroups(user?.id || 0);
  > 12 |   const { data: transactions, isLoading: txLoading } = useAjoTransactions(user?.id || 0);
       |                                                                           ^^^^^^^^^^^^^
    13 |   const { data: graceNotices, isLoading: noticesLoading } = useAjoGraceNotices(user?.id || 0);
    14 |
    15 |   const chartData = transactions?.slice(0, 7).reverse().map((tx, idx) => ({
ERROR in src/pages/AjoDashboardEnhanced.tsx:13:80
TS2345: Argument of type 'string | 0' is not assignable to parameter of type 'number'.
  Type 'string' is not assignable to type 'number'.
    11 |   const { data: groups, isLoading: groupsLoading } = useAjoGroups(user?.id || 0);
    12 |   const { data: transactions, isLoading: txLoading } = useAjoTransactions(user?.id || 0);
  > 13 |   const { data: graceNotices, isLoading: noticesLoading } = useAjoGraceNotices(user?.id || 0);
       |                                                                                ^^^^^^^^^^^^^
    14 |
    15 |   const chartData = transactions?.slice(0, 7).reverse().map((tx, idx) => ({
    16 |     name: `Day ${idx + 1}`,
ERROR in src/pages/AjoGroupDetail.tsx:3:26
TS2307: Cannot find module 'react-query' or its corresponding type declarations.
    1 | import React, { useState } from 'react';
    2 | import { useParams } from 'react-router-dom';
  > 3 | import { useQuery } from 'react-query';
      |                          ^^^^^^^^^^^^^
    4 | import { ajoGroupService } from '../services/ajoGroupService';
    5 | import { UserLayout } from '../components/UserLayout';
    6 | import { LoadingSpinner } from '../components/LoadingSpinner';
ERROR in src/pages/AjoGroupDetail.tsx:5:10
TS2614: Module '"../components/UserLayout"' has no exported member 'UserLayout'. Did you mean to use 'import UserLayout from "../components/UserLayout"' instead?
    3 | import { useQuery } from 'react-query';
    4 | import { ajoGroupService } from '../services/ajoGroupService';
  > 5 | import { UserLayout } from '../components/UserLayout';
      |          ^^^^^^^^^^
    6 | import { LoadingSpinner } from '../components/LoadingSpinner';
    7 | import { Alert } from '../components/Alert';
    8 |
ERROR in src/pages/AjoGroupDetail.tsx:6:10
TS2614: Module '"../components/LoadingSpinner"' has no exported member 'LoadingSpinner'. Did you mean to use 'import LoadingSpinner from "../components/LoadingSpinner"' instead?
    4 | import { ajoGroupService } from '../services/ajoGroupService';
    5 | import { UserLayout } from '../components/UserLayout';
  > 6 | import { LoadingSpinner } from '../components/LoadingSpinner';
      |          ^^^^^^^^^^^^^^
    7 | import { Alert } from '../components/Alert';
    8 |
    9 | export default function AjoGroupDetail() {
ERROR in src/pages/AjoGroupDetail.tsx:7:10
TS2614: Module '"../components/Alert"' has no exported member 'Alert'. Did you mean to use 'import Alert from "../components/Alert"' instead?
     5 | import { UserLayout } from '../components/UserLayout';
     6 | import { LoadingSpinner } from '../components/LoadingSpinner';
  >  7 | import { Alert } from '../components/Alert';
       |          ^^^^^
     8 |
     9 | export default function AjoGroupDetail() {
    10 |   const { groupId } = useParams<{ groupId: string }>();
ERROR in src/pages/AjoGroups.tsx:2:26
TS2307: Cannot find module 'react-query' or its corresponding type declarations.
    1 | import React, { useState, useEffect } from 'react';
  > 2 | import { useQuery } from 'react-query';
      |                          ^^^^^^^^^^^^^
    3 | import { useAuth } from '../contexts/AuthContext';
    4 | import { ajoGroupBrowseService, GroupBrowseFilter } from '../services/ajoGroupBrowseService';
    5 | import { ajoJoinRequestService } from '../services/ajoJoinRequestService';
ERROR in src/pages/AjoGroups.tsx:6:10
TS2614: Module '"../components/UserLayout"' has no exported member 'UserLayout'. Did you mean to use 'import UserLayout from "../components/UserLayout"' instead?
    4 | import { ajoGroupBrowseService, GroupBrowseFilter } from '../services/ajoGroupBrowseService';
    5 | import { ajoJoinRequestService } from '../services/ajoJoinRequestService';
  > 6 | import { UserLayout } from '../components/UserLayout';
      |          ^^^^^^^^^^
    7 | import { LoadingSpinner } from '../components/LoadingSpinner';
    8 | import { Alert } from '../components/Alert';
    9 | import { GroupBrowseCard } from '../components/ajo/GroupBrowseCard';
ERROR in src/pages/AjoGroups.tsx:7:10
TS2614: Module '"../components/LoadingSpinner"' has no exported member 'LoadingSpinner'. Did you mean to use 'import LoadingSpinner from "../components/LoadingSpinner"' instead?
     5 | import { ajoJoinRequestService } from '../services/ajoJoinRequestService';
     6 | import { UserLayout } from '../components/UserLayout';
  >  7 | import { LoadingSpinner } from '../components/LoadingSpinner';
       |          ^^^^^^^^^^^^^^
     8 | import { Alert } from '../components/Alert';
     9 | import { GroupBrowseCard } from '../components/ajo/GroupBrowseCard';
    10 | import { JoinRequestModal } from '../components/ajo/JoinRequestModal';
ERROR in src/pages/AjoGroups.tsx:8:10
TS2614: Module '"../components/Alert"' has no exported member 'Alert'. Did you mean to use 'import Alert from "../components/Alert"' instead?
     6 | import { UserLayout } from '../components/UserLayout';
     7 | import { LoadingSpinner } from '../components/LoadingSpinner';
  >  8 | import { Alert } from '../components/Alert';
       |          ^^^^^
     9 | import { GroupBrowseCard } from '../components/ajo/GroupBrowseCard';
    10 | import { JoinRequestModal } from '../components/ajo/JoinRequestModal';
    11 |
ERROR in src/pages/AjoGroups.tsx:45:52
TS2339: Property 'getUserJoinRequests' does not exist on type '{ submitJoinRequest(userId: number, groupId: number): Promise<{ data: any; error: any; } | { data: any; error: any; }>; approveJoinRequest(requestId: number, adminId: number): Promise<...>; ... 5 more ...; subscribeToJoinRequests(groupId: number, callback: (request: any) => void): RealtimeChannel; }'.
    43 |     async () => {
    44 |       if (!user?.id) return [];
  > 45 |       const { data } = await ajoJoinRequestService.getUserJoinRequests(user.id);
       |                                                    ^^^^^^^^^^^^^^^^^^^
    46 |       return data || [];
    47 |     },
    48 |     { enabled: !!user?.id }
ERROR in src/pages/AjoGroups.tsx:345:11
TS2322: Type 'string | number' is not assignable to type 'number'.
  Type 'string' is not assignable to type 'number'.
    343 |           groupId={selectedGroup.id}
    344 |           groupName={selectedGroup.name}
  > 345 |           userId={user?.id || 0}
        |           ^^^^^^
    346 |           onClose={() => {
    347 |             setShowJoinModal(false);
    348 |             setSelectedGroup(null);
ERROR in src/pages/AjoOnboarding.tsx:4:10
TS2614: Module '"../components/UserLayout"' has no exported member 'UserLayout'. Did you mean to use 'import UserLayout from "../components/UserLayout"' instead?
    2 | import { useNavigate } from 'react-router-dom';
    3 | import { ajoService } from '../services/ajoService';
  > 4 | import { UserLayout } from '../components/UserLayout';
      |          ^^^^^^^^^^
    5 | import { Alert } from '../components/Alert';
    6 |
    7 | type AjoType = 'personal' | 'group';
ERROR in src/pages/AjoOnboarding.tsx:5:10
TS2614: Module '"../components/Alert"' has no exported member 'Alert'. Did you mean to use 'import Alert from "../components/Alert"' instead?
    3 | import { ajoService } from '../services/ajoService';
    4 | import { UserLayout } from '../components/UserLayout';
  > 5 | import { Alert } from '../components/Alert';
      |          ^^^^^
    6 |
    7 | type AjoType = 'personal' | 'group';
    8 |
ERROR in src/pages/AjoOnboarding.tsx:47:49
TS2345: Argument of type 'FormData' is not assignable to parameter of type 'CreateAjoData'.
  Type 'FormData' is missing the following properties from type 'CreateAjoData': paymentReference, firstPayment
    45 |     setError('');
    46 |     try {
  > 47 |       const result = await ajoService.createAjo(formData);
       |                                                 ^^^^^^^^
    48 |       navigate('/ajo/dashboard?success=created');
    49 |     } catch (err: any) {
    50 |       setError(err.message || 'Failed to create Ajo');
ERROR in src/pages/AjoPaymentCallback.tsx:10:21
TS2339: Property 'resetForm' does not exist on type 'OnboardingContextType'.
     8 |   const navigate = useNavigate();
     9 |   const { user } = useAuth();
  > 10 |   const { formData, resetForm } = useOnboarding();
       |                     ^^^^^^^^^
    11 |   const [searchParams] = useSearchParams();
    12 |   const [isProcessing, setIsProcessing] = useState(true);
    13 |
ERROR in src/pages/AjoPaymentCallback.tsx:40:17
TS2345: Argument of type 'string' is not assignable to parameter of type 'number'.
    38 |             try {
    39 |               await ajoPaymentService.createAjoAfterPayment(
  > 40 |                 user.id,
       |                 ^^^^^^^
    41 |                 formData,
    42 |                 verification.data.tx_ref
    43 |               );
ERROR in src/pages/AjoProfile.tsx:2:26
TS2307: Cannot find module 'react-query' or its corresponding type declarations.
    1 | import React from 'react';
  > 2 | import { useQuery } from 'react-query';
      |                          ^^^^^^^^^^^^^
    3 | import { supabase } from '../lib/supabase';
    4 | import { UserLayout } from '../components/UserLayout';
    5 | import { LoadingSpinner } from '../components/LoadingSpinner';
ERROR in src/pages/AjoProfile.tsx:4:10
TS2614: Module '"../components/UserLayout"' has no exported member 'UserLayout'. Did you mean to use 'import UserLayout from "../components/UserLayout"' instead?
    2 | import { useQuery } from 'react-query';
    3 | import { supabase } from '../lib/supabase';
  > 4 | import { UserLayout } from '../components/UserLayout';
      |          ^^^^^^^^^^
    5 | import { LoadingSpinner } from '../components/LoadingSpinner';
    6 | import { Alert } from '../components/Alert';
    7 |
ERROR in src/pages/AjoProfile.tsx:5:10
TS2614: Module '"../components/LoadingSpinner"' has no exported member 'LoadingSpinner'. Did you mean to use 'import LoadingSpinner from "../components/LoadingSpinner"' instead?
    3 | import { supabase } from '../lib/supabase';
    4 | import { UserLayout } from '../components/UserLayout';
  > 5 | import { LoadingSpinner } from '../components/LoadingSpinner';
      |          ^^^^^^^^^^^^^^
    6 | import { Alert } from '../components/Alert';
    7 |
    8 | export default function AjoProfile() {
ERROR in src/pages/AjoProfile.tsx:6:10
TS2614: Module '"../components/Alert"' has no exported member 'Alert'. Did you mean to use 'import Alert from "../components/Alert"' instead?
    4 | import { UserLayout } from '../components/UserLayout';
    5 | import { LoadingSpinner } from '../components/LoadingSpinner';
  > 6 | import { Alert } from '../components/Alert';
      |          ^^^^^
    7 |
    8 | export default function AjoProfile() {
    9 |   // Fetch user profile
ERROR in src/pages/AjoWithdraw.tsx:3:26
TS2307: Cannot find module 'react-query' or its corresponding type declarations.
    1 | import React, { useState, useEffect } from 'react';
    2 | import { useParams, useNavigate } from 'react-router-dom';
  > 3 | import { useQuery } from 'react-query';
      |                          ^^^^^^^^^^^^^
    4 | import { ajoGroupService } from '../services/ajoGroupService';
    5 | import { ajoPositionService } from '../services/ajoPositionService';
    6 | import { UserLayout } from '../components/UserLayout';
ERROR in src/pages/AjoWithdraw.tsx:6:10
TS2614: Module '"../components/UserLayout"' has no exported member 'UserLayout'. Did you mean to use 'import UserLayout from "../components/UserLayout"' instead?
    4 | import { ajoGroupService } from '../services/ajoGroupService';
    5 | import { ajoPositionService } from '../services/ajoPositionService';
  > 6 | import { UserLayout } from '../components/UserLayout';
      |          ^^^^^^^^^^
    7 | import { LoadingSpinner } from '../components/LoadingSpinner';
    8 | import { Alert } from '../components/Alert';
    9 |
ERROR in src/pages/AjoWithdraw.tsx:7:10
TS2614: Module '"../components/LoadingSpinner"' has no exported member 'LoadingSpinner'. Did you mean to use 'import LoadingSpinner from "../components/LoadingSpinner"' instead?
     5 | import { ajoPositionService } from '../services/ajoPositionService';
     6 | import { UserLayout } from '../components/UserLayout';
  >  7 | import { LoadingSpinner } from '../components/LoadingSpinner';
       |          ^^^^^^^^^^^^^^
     8 | import { Alert } from '../components/Alert';
     9 |
    10 | interface WithdrawalState {
ERROR in src/pages/AjoWithdraw.tsx:8:10
TS2614: Module '"../components/Alert"' has no exported member 'Alert'. Did you mean to use 'import Alert from "../components/Alert"' instead?
     6 | import { UserLayout } from '../components/UserLayout';
     7 | import { LoadingSpinner } from '../components/LoadingSpinner';
  >  8 | import { Alert } from '../components/Alert';
       |          ^^^^^
     9 |
    10 | interface WithdrawalState {
    11 |   withdrawalType: 'full' | 'partial';
ERROR in src/pages/AjoWithdraw.tsx:45:43
TS2304: Cannot find name 'supabase'.
    43 |     async () => {
    44 |       if (!cycle) return null;
  > 45 |       const { data: { session } } = await supabase.auth.getSession();
       |                                           ^^^^^^^^
    46 |       if (!session) return null;
    47 |       
    48 |       const { data: userData } = await supabase
ERROR in src/pages/AjoWithdraw.tsx:48:40
TS2304: Cannot find name 'supabase'.
    46 |       if (!session) return null;
    47 |       
  > 48 |       const { data: userData } = await supabase
       |                                        ^^^^^^^^
    49 |         .from('user_accounts')
    50 |         .select('Id')
    51 |         .eq('Email', session.user.email)
ERROR in src/services/ajoJoinRequestService.ts:63:10
TS2551: Property 'catch' does not exist on type 'PostgrestFilterBuilder<any, any, any, any, "log_ajo_event", null, "RPC">'. Did you mean 'match'?
    61 |         event: 'join_request_submitted',
    62 |         data: { user_id: userId, group_id: groupId },
  > 63 |       }).catch(() => {});
       |          ^^^^^
    64 |
    65 |       return { data, error: null };
    66 |     } catch (error) {
ERROR in src/services/ajoJoinRequestService.ts:128:10
TS2551: Property 'catch' does not exist on type 'PostgrestFilterBuilder<any, any, any, any, "increment_group_members", null, "RPC">'. Did you mean 'match'?
    126 |       await supabase.rpc('increment_group_members', {
    127 |         p_group_id: request.group_id,
  > 128 |       }).catch(() => {});
        |          ^^^^^
    129 |
    130 |       // Send notification
    131 |       await supabase.rpc('notify_user', {
ERROR in src/services/ajoJoinRequestService.ts:136:10
TS2551: Property 'catch' does not exist on type 'PostgrestFilterBuilder<any, any, any, any, "notify_user", null, "RPC">'. Did you mean 'match'?
    134 |         p_message: 'Your request to join the group has been approved!',
    135 |         p_type: 'ajo_join_approved',
  > 136 |       }).catch(() => {});
        |          ^^^^^
    137 |
    138 |       // Log event
    139 |       await supabase.rpc('log_ajo_event', {
ERROR in src/services/ajoJoinRequestService.ts:142:10
TS2551: Property 'catch' does not exist on type 'PostgrestFilterBuilder<any, any, any, any, "log_ajo_event", null, "RPC">'. Did you mean 'match'?
    140 |         event: 'join_request_approved',
    141 |         data: { request_id: requestId, user_id: request.user_id, group_id: request.group_id },
  > 142 |       }).catch(() => {});
        |          ^^^^^
    143 |
    144 |       return { data: updated, error: null };
    145 |     } catch (error) {
ERROR in src/services/ajoJoinRequestService.ts:186:10
TS2551: Property 'catch' does not exist on type 'PostgrestFilterBuilder<any, any, any, any, "notify_user", null, "RPC">'. Did you mean 'match'?
    184 |         p_message: reason || 'Your request to join the group was not approved.',
    185 |         p_type: 'ajo_join_rejected',
  > 186 |       }).catch(() => {});
        |          ^^^^^
    187 |
    188 |       // Log event
    189 |       await supabase.rpc('log_ajo_event', {
ERROR in src/services/ajoJoinRequestService.ts:192:10
TS2551: Property 'catch' does not exist on type 'PostgrestFilterBuilder<any, any, any, any, "log_ajo_event", null, "RPC">'. Did you mean 'match'?
    190 |         event: 'join_request_rejected',
    191 |         data: { request_id: requestId, user_id: request.user_id, group_id: request.group_id, reason },
  > 192 |       }).catch(() => {});
        |          ^^^^^
    193 |
    194 |       return { data: updated, error: null };
    195 |     } catch (error) {
ERROR in src/services/ajoJoinRequestService.ts:228:10
TS2551: Property 'catch' does not exist on type 'PostgrestFilterBuilder<any, any, any, any, "log_ajo_event", null, "RPC">'. Did you mean 'match'?
    226 |         event: 'join_request_cancelled',
    227 |         data: { request_id: requestId, user_id: userId, group_id: request.group_id },
  > 228 |       }).catch(() => {});
        |          ^^^^^
    229 |
    230 |       return { data: { success: true }, error: null };
    231 |     } catch (error) {
ERROR in src/services/ajoMemberProfileService.ts:97:10
TS2551: Property 'catch' does not exist on type 'PostgrestFilterBuilder<any, any, any, any, "log_ajo_event", null, "RPC">'. Did you mean 'match'?
     95 |         event: 'member_profile_updated',
     96 |         data: { user_id: userId, updates },
  >  97 |       }).catch(() => {});
        |          ^^^^^
     98 |
     99 |       return { data, error: null };
    100 |     } catch (error) {
ERROR in src/services/ajoMemberProfileService.ts:153:10
TS2551: Property 'catch' does not exist on type 'PostgrestFilterBuilder<any, any, any, any, "log_ajo_event", null, "RPC">'. Did you mean 'match'?
    151 |         event: 'member_photo_uploaded',
    152 |         data: { user_id: userId, file_name: fileName },
  > 153 |       }).catch(() => {});
        |          ^^^^^
    154 |
    155 |       return { data: profile, error: null };
    156 |     } catch (error) {
ERROR in src/services/ajoMemberProfileService.ts:216:10
TS2551: Property 'catch' does not exist on type 'PostgrestFilterBuilder<any, any, any, any, "notify_user", null, "RPC">'. Did you mean 'match'?
    214 |         p_message: message,
    215 |         p_type: `kyc_${status}`,
  > 216 |       }).catch(() => {});
        |          ^^^^^
    217 |
    218 |       // Log event
    219 |       await supabase.rpc('log_ajo_event', {
ERROR in src/services/ajoMemberProfileService.ts:222:10
TS2551: Property 'catch' does not exist on type 'PostgrestFilterBuilder<any, any, any, any, "log_ajo_event", null, "RPC">'. Did you mean 'match'?
    220 |         event: 'kyc_verified',
    221 |         data: { user_id: userId, status, admin_id: adminId },
  > 222 |       }).catch(() => {});
        |          ^^^^^
    223 |
    224 |       return { data, error: null };
    225 |     } catch (error) {
ERROR in src/services/ajoNotificationService.ts:33:69
TS2339: Property 'name' does not exist on type '{ name: any; }[]'.
    31 |       // Pidgin message
    32 |       const pidginMessages = {
  > 33 |         3: `Bros/Sis, your Ajo contribution for ${member.ajo_groups.name} don miss deadline. You get 3 days grace period to pay sharp sharp! No wahala, just settle am quick quick.`,
       |                                                                     ^^^^
    34 |         2: `Oga/Madam! Last 2 days to settle your Ajo contribution for ${member.ajo_groups.name}. E don reach final warning o! Pay now before e turn to default.`,
    35 |         1: `URGENT! Just 1 day left to pay your Ajo contribution for ${member.ajo_groups.name}. After today, na default! Abeg pay now now!`
    36 |       };
ERROR in src/services/ajoNotificationService.ts:34:92
TS2339: Property 'name' does not exist on type '{ name: any; }[]'.
    32 |       const pidginMessages = {
    33 |         3: `Bros/Sis, your Ajo contribution for ${member.ajo_groups.name} don miss deadline. You get 3 days grace period to pay sharp sharp! No wahala, just settle am quick quick.`,
  > 34 |         2: `Oga/Madam! Last 2 days to settle your Ajo contribution for ${member.ajo_groups.name}. E don reach final warning o! Pay now before e turn to default.`,
       |                                                                                            ^^^^
    35 |         1: `URGENT! Just 1 day left to pay your Ajo contribution for ${member.ajo_groups.name}. After today, na default! Abeg pay now now!`
    36 |       };
    37 |
ERROR in src/services/ajoNotificationService.ts:35:90
TS2339: Property 'name' does not exist on type '{ name: any; }[]'.
    33 |         3: `Bros/Sis, your Ajo contribution for ${member.ajo_groups.name} don miss deadline. You get 3 days grace period to pay sharp sharp! No wahala, just settle am quick quick.`,
    34 |         2: `Oga/Madam! Last 2 days to settle your Ajo contribution for ${member.ajo_groups.name}. E don reach final warning o! Pay now before e turn to default.`,
  > 35 |         1: `URGENT! Just 1 day left to pay your Ajo contribution for ${member.ajo_groups.name}. After today, na default! Abeg pay now now!`
       |                                                                                          ^^^^
    36 |       };
    37 |
    38 |       const message = pidginMessages[daysRemaining as keyof typeof pidginMessages] || 
ERROR in src/services/ajoNotificationService.ts:84:36
TS2339: Property 'phone_number' does not exist on type '{ phone_number: any; }[]'.
    82 |       // Send to each member
    83 |       for (const member of members) {
  > 84 |         if (!member.user_accounts?.phone_number) continue;
       |                                    ^^^^^^^^^^^^
    85 |
    86 |         const message = `Reminder: Your Ajo contribution of ₦${cycle.ajo_groups.contribution_amount} for ${cycle.ajo_groups.name} is due by ${new Date(cycle.contribution_deadline).toLocaleDateString()}. Pay now to maintain your reliability score!`;
    87 |
ERROR in src/services/ajoNotificationService.ts:86:81
TS2339: Property 'contribution_amount' does not exist on type '{ name: any; contribution_amount: any; }[]'.
    84 |         if (!member.user_accounts?.phone_number) continue;
    85 |
  > 86 |         const message = `Reminder: Your Ajo contribution of ₦${cycle.ajo_groups.contribution_amount} for ${cycle.ajo_groups.name} is due by ${new Date(cycle.contribution_deadline).toLocaleDateString()}. Pay now to maintain your reliability score!`;
       |                                                                                 ^^^^^^^^^^^^^^^^^^^
    87 |
    88 |         await ajoNotificationService.sendSMS({
    89 |           phone: member.user_accounts.phone_number,
ERROR in src/services/ajoNotificationService.ts:86:125
TS2339: Property 'name' does not exist on type '{ name: any; contribution_amount: any; }[]'.
    84 |         if (!member.user_accounts?.phone_number) continue;
    85 |
  > 86 |         const message = `Reminder: Your Ajo contribution of ₦${cycle.ajo_groups.contribution_amount} for ${cycle.ajo_groups.name} is due by ${new Date(cycle.contribution_deadline).toLocaleDateString()}. Pay now to maintain your reliability score!`;
       |                                                                                                                             ^^^^
    87 |
    88 |         await ajoNotificationService.sendSMS({
    89 |           phone: member.user_accounts.phone_number,
ERROR in src/services/ajoNotificationService.ts:89:39
TS2339: Property 'phone_number' does not exist on type '{ phone_number: any; }[]'.
    87 |
    88 |         await ajoNotificationService.sendSMS({
  > 89 |           phone: member.user_accounts.phone_number,
       |                                       ^^^^^^^^^^^^
    90 |           message,
    91 |           type: 'contribution_due'
    92 |         });
ERROR in src/services/ajoNotificationService.ts:121:96
TS2339: Property 'name' does not exist on type '{ name: any; }[]'.
    119 |       if (!user?.phone_number) throw new Error('User phone not found');
    120 |
  > 121 |       const message = `Congratulations! Your Ajo payout of ₦${amount} from ${member.ajo_groups.name} is ready! Go to your dashboard to withdraw. E don pay o!`;
        |                                                                                                ^^^^
    122 |
    123 |       await ajoNotificationService.sendSMS({
    124 |         phone: user.phone_number,
ERROR in src/services/ajoNotificationService.ts:155:70
TS2339: Property 'name' does not exist on type '{ name: any; }[]'.
    153 |       if (!user?.phone_number) throw new Error('User phone not found');
    154 |
  > 155 |       const message = `Your Ajo contribution for ${member.ajo_groups.name} has been marked as DEFAULT. Your reliability score has been affected. Contact group admin to resolve.`;
        |                                                                      ^^^^
    156 |
    157 |       await ajoNotificationService.sendSMS({
    158 |         phone: user.phone_number,
ERROR in src/services/ajoPaymentService.ts:2:10
TS2305: Module '"../types/ajo"' has no exported member 'AjoFormData'.
    1 | import { supabase } from '../lib/supabase';
  > 2 | import { AjoFormData } from '../types/ajo';
      |          ^^^^^^^^^^^
    3 |
    4 | export interface PaymentInitRequest {
    5 |   email: string; 46 |       return data || [];
    47 |     },
    48 |     { enabled: !!user?.id }
ERROR in src/pages/AjoGroups.tsx:345:11
TS2322: Type 'string | number' is not assignable to type 'number'.
  Type 'string' is not assignable to type 'number'.
    343 |           groupId={selectedGroup.id}
    344 |           groupName={selectedGroup.name}
  > 345 |           userId={user?.id || 0}
        |           ^^^^^^
    346 |           onClose={() => {
    347 |             setShowJoinModal(false);
    348 |             setSelectedGroup(null);
ERROR in src/pages/AjoOnboarding.tsx:4:10
TS2614: Module '"../components/UserLayout"' has no exported member 'UserLayout'. Did you mean to use 'import UserLayout from "../components/UserLayout"' instead?
    2 | import { useNavigate } from 'react-router-dom';
    3 | import { ajoService } from '../services/ajoService';
  > 4 | import { UserLayout } from '../components/UserLayout';
      |          ^^^^^^^^^^
    5 | import { Alert } from '../components/Alert';
    6 |
    7 | type AjoType = 'personal' | 'group';
ERROR in src/pages/AjoOnboarding.tsx:5:10
TS2614: Module '"../components/Alert"' has no exported member 'Alert'. Did you mean to use 'import Alert from "../components/Alert"' instead?
    3 | import { ajoService } from '../services/ajoService';
    4 | import { UserLayout } from '../components/UserLayout';
  > 5 | import { Alert } from '../components/Alert';
      |          ^^^^^
    6 |
    7 | type AjoType = 'personal' | 'group';
    8 |
ERROR in src/pages/AjoOnboarding.tsx:47:49
TS2345: Argument of type 'FormData' is not assignable to parameter of type 'CreateAjoData'.
  Type 'FormData' is missing the following properties from type 'CreateAjoData': paymentReference, firstPayment
    45 |     setError('');
    46 |     try {
  > 47 |       const result = await ajoService.createAjo(formData);
       |                                                 ^^^^^^^^
    48 |       navigate('/ajo/dashboard?success=created');
    49 |     } catch (err: any) {
    50 |       setError(err.message || 'Failed to create Ajo');
ERROR in src/pages/AjoPaymentCallback.tsx:10:21
TS2339: Property 'resetForm' does not exist on type 'OnboardingContextType'.
     8 |   const navigate = useNavigate();
     9 |   const { user } = useAuth();
  > 10 |   const { formData, resetForm } = useOnboarding();
       |                     ^^^^^^^^^
    11 |   const [searchParams] = useSearchParams();
    12 |   const [isProcessing, setIsProcessing] = useState(true);
    13 |
ERROR in src/pages/AjoPaymentCallback.tsx:40:17
TS2345: Argument of type 'string' is not assignable to parameter of type 'number'.
    38 |             try {
    39 |               await ajoPaymentService.createAjoAfterPayment(
  > 40 |                 user.id,
       |                 ^^^^^^^
    41 |                 formData,
    42 |                 verification.data.tx_ref
    43 |               );
ERROR in src/pages/AjoProfile.tsx:2:26
TS2307: Cannot find module 'react-query' or its corresponding type declarations.
    1 | import React from 'react';
  > 2 | import { useQuery } from 'react-query';
      |                          ^^^^^^^^^^^^^
    3 | import { supabase } from '../lib/supabase';
    4 | import { UserLayout } from '../components/UserLayout';
    5 | import { LoadingSpinner } from '../components/LoadingSpinner';
ERROR in src/pages/AjoProfile.tsx:4:10
TS2614: Module '"../components/UserLayout"' has no exported member 'UserLayout'. Did you mean to use 'import UserLayout from "../components/UserLayout"' instead?
    2 | import { useQuery } from 'react-query';
    3 | import { supabase } from '../lib/supabase';
  > 4 | import { UserLayout } from '../components/UserLayout';
      |          ^^^^^^^^^^
    5 | import { LoadingSpinner } from '../components/LoadingSpinner';
    6 | import { Alert } from '../components/Alert';
    7 |
ERROR in src/pages/AjoProfile.tsx:5:10
TS2614: Module '"../components/LoadingSpinner"' has no exported member 'LoadingSpinner'. Did you mean to use 'import LoadingSpinner from "../components/LoadingSpinner"' instead?
    3 | import { supabase } from '../lib/supabase';
    4 | import { UserLayout } from '../components/UserLayout';
  > 5 | import { LoadingSpinner } from '../components/LoadingSpinner';
      |          ^^^^^^^^^^^^^^
    6 | import { Alert } from '../components/Alert';
    7 |
    8 | export default function AjoProfile() {
ERROR in src/pages/AjoProfile.tsx:6:10
TS2614: Module '"../components/Alert"' has no exported member 'Alert'. Did you mean to use 'import Alert from "../components/Alert"' instead?
    4 | import { UserLayout } from '../components/UserLayout';
    5 | import { LoadingSpinner } from '../components/LoadingSpinner';
  > 6 | import { Alert } from '../components/Alert';
      |          ^^^^^
    7 |
    8 | export default function AjoProfile() {
    9 |   // Fetch user profile
ERROR in src/pages/AjoWithdraw.tsx:3:26
TS2307: Cannot find module 'react-query' or its corresponding type declarations.
    1 | import React, { useState, useEffect } from 'react';
    2 | import { useParams, useNavigate } from 'react-router-dom';
  > 3 | import { useQuery } from 'react-query';
      |                          ^^^^^^^^^^^^^
    4 | import { ajoGroupService } from '../services/ajoGroupService';
    5 | import { ajoPositionService } from '../services/ajoPositionService';
    6 | import { UserLayout } from '../components/UserLayout';
ERROR in src/pages/AjoWithdraw.tsx:6:10
TS2614: Module '"../components/UserLayout"' has no exported member 'UserLayout'. Did you mean to use 'import UserLayout from "../components/UserLayout"' instead?
    4 | import { ajoGroupService } from '../services/ajoGroupService';
    5 | import { ajoPositionService } from '../services/ajoPositionService';
  > 6 | import { UserLayout } from '../components/UserLayout';
      |          ^^^^^^^^^^
    7 | import { LoadingSpinner } from '../components/LoadingSpinner';
    8 | import { Alert } from '../components/Alert';
    9 |
ERROR in src/pages/AjoWithdraw.tsx:7:10
TS2614: Module '"../components/LoadingSpinner"' has no exported member 'LoadingSpinner'. Did you mean to use 'import LoadingSpinner from "../components/LoadingSpinner"' instead?
     5 | import { ajoPositionService } from '../services/ajoPositionService';
     6 | import { UserLayout } from '../components/UserLayout';
  >  7 | import { LoadingSpinner } from '../components/LoadingSpinner';
       |          ^^^^^^^^^^^^^^
     8 | import { Alert } from '../components/Alert';
     9 |
    10 | interface WithdrawalState {
ERROR in src/pages/AjoWithdraw.tsx:8:10
TS2614: Module '"../components/Alert"' has no exported member 'Alert'. Did you mean to use 'import Alert from "../components/Alert"' instead?
     6 | import { UserLayout } from '../components/UserLayout';
     7 | import { LoadingSpinner } from '../components/LoadingSpinner';
  >  8 | import { Alert } from '../components/Alert';
       |          ^^^^^
     9 |
    10 | interface WithdrawalState {
    11 |   withdrawalType: 'full' | 'partial';
ERROR in src/pages/AjoWithdraw.tsx:45:43
TS2304: Cannot find name 'supabase'.
    43 |     async () => {
    44 |       if (!cycle) return null;
  > 45 |       const { data: { session } } = await supabase.auth.getSession();
       |                                           ^^^^^^^^
    46 |       if (!session) return null;
    47 |       
    48 |       const { data: userData } = await supabase
ERROR in src/pages/AjoWithdraw.tsx:48:40
TS2304: Cannot find name 'supabase'.
    46 |       if (!session) return null;
    47 |       
  > 48 |       const { data: userData } = await supabase
       |                                        ^^^^^^^^
    49 |         .from('user_accounts')
    50 |         .select('Id')
    51 |         .eq('Email', session.user.email)
ERROR in src/services/ajoJoinRequestService.ts:63:10
TS2551: Property 'catch' does not exist on type 'PostgrestFilterBuilder<any, any, any, any, "log_ajo_event", null, "RPC">'. Did you mean 'match'?
    61 |         event: 'join_request_submitted',
    62 |         data: { user_id: userId, group_id: groupId },
  > 63 |       }).catch(() => {});
       |          ^^^^^
    64 |
    65 |       return { data, error: null };
    66 |     } catch (error) {
ERROR in src/services/ajoJoinRequestService.ts:128:10
TS2551: Property 'catch' does not exist on type 'PostgrestFilterBuilder<any, any, any, any, "increment_group_members", null, "RPC">'. Did you mean 'match'?
    126 |       await supabase.rpc('increment_group_members', {
    127 |         p_group_id: request.group_id,
  > 128 |       }).catch(() => {});
        |          ^^^^^
    129 |
    130 |       // Send notification
    131 |       await supabase.rpc('notify_user', {
ERROR in src/services/ajoJoinRequestService.ts:136:10
TS2551: Property 'catch' does not exist on type 'PostgrestFilterBuilder<any, any, any, any, "notify_user", null, "RPC">'. Did you mean 'match'?
    134 |         p_message: 'Your request to join the group has been approved!',
    135 |         p_type: 'ajo_join_approved',
  > 136 |       }).catch(() => {});
        |          ^^^^^
    137 |
    138 |       // Log event
    139 |       await supabase.rpc('log_ajo_event', {
ERROR in src/services/ajoJoinRequestService.ts:142:10
TS2551: Property 'catch' does not exist on type 'PostgrestFilterBuilder<any, any, any, any, "log_ajo_event", null, "RPC">'. Did you mean 'match'?
    140 |         event: 'join_request_approved',
    141 |         data: { request_id: requestId, user_id: request.user_id, group_id: request.group_id },
  > 142 |       }).catch(() => {});
        |          ^^^^^
    143 |
    144 |       return { data: updated, error: null };
    145 |     } catch (error) {
ERROR in src/services/ajoJoinRequestService.ts:186:10
TS2551: Property 'catch' does not exist on type 'PostgrestFilterBuilder<any, any, any, any, "notify_user", null, "RPC">'. Did you mean 'match'?
    184 |         p_message: reason || 'Your request to join the group was not approved.',
    185 |         p_type: 'ajo_join_rejected',
  > 186 |       }).catch(() => {});
        |          ^^^^^
    187 |
    188 |       // Log event
    189 |       await supabase.rpc('log_ajo_event', {
ERROR in src/services/ajoJoinRequestService.ts:192:10
TS2551: Property 'catch' does not exist on type 'PostgrestFilterBuilder<any, any, any, any, "log_ajo_event", null, "RPC">'. Did you mean 'match'?
    190 |         event: 'join_request_rejected',
    191 |         data: { request_id: requestId, user_id: request.user_id, group_id: request.group_id, reason },
  > 192 |       }).catch(() => {});
        |          ^^^^^
    193 |
    194 |       return { data: updated, error: null };
    195 |     } catch (error) {
ERROR in src/services/ajoJoinRequestService.ts:228:10
TS2551: Property 'catch' does not exist on type 'PostgrestFilterBuilder<any, any, any, any, "log_ajo_event", null, "RPC">'. Did you mean 'match'?
    226 |         event: 'join_request_cancelled',
    227 |         data: { request_id: requestId, user_id: userId, group_id: request.group_id },
  > 228 |       }).catch(() => {});
        |          ^^^^^
    229 |
    230 |       return { data: { success: true }, error: null };
    231 |     } catch (error) {
ERROR in src/services/ajoMemberProfileService.ts:97:10
TS2551: Property 'catch' does not exist on type 'PostgrestFilterBuilder<any, any, any, any, "log_ajo_event", null, "RPC">'. Did you mean 'match'?
     95 |         event: 'member_profile_updated',
     96 |         data: { user_id: userId, updates },
  >  97 |       }).catch(() => {});
        |          ^^^^^
     98 |
     99 |       return { data, error: null };
    100 |     } catch (error) {
ERROR in src/services/ajoMemberProfileService.ts:153:10
TS2551: Property 'catch' does not exist on type 'PostgrestFilterBuilder<any, any, any, any, "log_ajo_event", null, "RPC">'. Did you mean 'match'?
    151 |         event: 'member_photo_uploaded',
    152 |         data: { user_id: userId, file_name: fileName },
  > 153 |       }).catch(() => {});
        |          ^^^^^
    154 |
    155 |       return { data: profile, error: null };
    156 |     } catch (error) {
ERROR in src/services/ajoMemberProfileService.ts:216:10
TS2551: Property 'catch' does not exist on type 'PostgrestFilterBuilder<any, any, any, any, "notify_user", null, "RPC">'. Did you mean 'match'?
    214 |         p_message: message,
    215 |         p_type: `kyc_${status}`,
  > 216 |       }).catch(() => {});
        |          ^^^^^
    217 |
    218 |       // Log event
    219 |       await supabase.rpc('log_ajo_event', {
ERROR in src/services/ajoMemberProfileService.ts:222:10
TS2551: Property 'catch' does not exist on type 'PostgrestFilterBuilder<any, any, any, any, "log_ajo_event", null, "RPC">'. Did you mean 'match'?
    220 |         event: 'kyc_verified',
    221 |         data: { user_id: userId, status, admin_id: adminId },
  > 222 |       }).catch(() => {});
        |          ^^^^^
    223 |
    224 |       return { data, error: null };
    225 |     } catch (error) {
ERROR in src/services/ajoNotificationService.ts:33:69
TS2339: Property 'name' does not exist on type '{ name: any; }[]'.
    31 |       // Pidgin message
    32 |       const pidginMessages = {
  > 33 |         3: `Bros/Sis, your Ajo contribution for ${member.ajo_groups.name} don miss deadline. You get 3 days grace period to pay sharp sharp! No wahala, just settle am quick quick.`,
       |                                                                     ^^^^
    34 |         2: `Oga/Madam! Last 2 days to settle your Ajo contribution for ${member.ajo_groups.name}. E don reach final warning o! Pay now before e turn to default.`,
    35 |         1: `URGENT! Just 1 day left to pay your Ajo contribution for ${member.ajo_groups.name}. After today, na default! Abeg pay now now!`
    36 |       };
ERROR in src/services/ajoNotificationService.ts:34:92
TS2339: Property 'name' does not exist on type '{ name: any; }[]'.
    32 |       const pidginMessages = {
    33 |         3: `Bros/Sis, your Ajo contribution for ${member.ajo_groups.name} don miss deadline. You get 3 days grace period to pay sharp sharp! No wahala, just settle am quick quick.`,
  > 34 |         2: `Oga/Madam! Last 2 days to settle your Ajo contribution for ${member.ajo_groups.name}. E don reach final warning o! Pay now before e turn to default.`,
       |                                                                                            ^^^^
    35 |         1: `URGENT! Just 1 day left to pay your Ajo contribution for ${member.ajo_groups.name}. After today, na default! Abeg pay now now!`
    36 |       };
    37 |
ERROR in src/services/ajoNotificationService.ts:35:90
TS2339: Property 'name' does not exist on type '{ name: any; }[]'.
    33 |         3: `Bros/Sis, your Ajo contribution for ${member.ajo_groups.name} don miss deadline. You get 3 days grace period to pay sharp sharp! No wahala, just settle am quick quick.`,
    34 |         2: `Oga/Madam! Last 2 days to settle your Ajo contribution for ${member.ajo_groups.name}. E don reach final warning o! Pay now before e turn to default.`,
  > 35 |         1: `URGENT! Just 1 day left to pay your Ajo contribution for ${member.ajo_groups.name}. After today, na default! Abeg pay now now!`
       |                                                                                          ^^^^
    36 |       };
    37 |
    38 |       const message = pidginMessages[daysRemaining as keyof typeof pidginMessages] || 
ERROR in src/services/ajoNotificationService.ts:84:36
TS2339: Property 'phone_number' does not exist on type '{ phone_number: any; }[]'.
    82 |       // Send to each member
    83 |       for (const member of members) {
  > 84 |         if (!member.user_accounts?.phone_number) continue;
       |                                    ^^^^^^^^^^^^
    85 |
    86 |         const message = `Reminder: Your Ajo contribution of ₦${cycle.ajo_groups.contribution_amount} for ${cycle.ajo_groups.name} is due by ${new Date(cycle.contribution_deadline).toLocaleDateString()}. Pay now to maintain your reliability score!`;
    87 |
ERROR in src/services/ajoNotificationService.ts:86:81
TS2339: Property 'contribution_amount' does not exist on type '{ name: any; contribution_amount: any; }[]'.
    84 |         if (!member.user_accounts?.phone_number) continue;
    85 |
  > 86 |         const message = `Reminder: Your Ajo contribution of ₦${cycle.ajo_groups.contribution_amount} for ${cycle.ajo_groups.name} is due by ${new Date(cycle.contribution_deadline).toLocaleDateString()}. Pay now to maintain your reliability score!`;
       |                                                                                 ^^^^^^^^^^^^^^^^^^^
    87 |
    88 |         await ajoNotificationService.sendSMS({
    89 |           phone: member.user_accounts.phone_number,
ERROR in src/services/ajoNotificationService.ts:86:125
TS2339: Property 'name' does not exist on type '{ name: any; contribution_amount: any; }[]'.
    84 |         if (!member.user_accounts?.phone_number) continue;
    85 |
  > 86 |         const message = `Reminder: Your Ajo contribution of ₦${cycle.ajo_groups.contribution_amount} for ${cycle.ajo_groups.name} is due by ${new Date(cycle.contribution_deadline).toLocaleDateString()}. Pay now to maintain your reliability score!`;
       |                                                                                                                             ^^^^
    87 |
    88 |         await ajoNotificationService.sendSMS({
    89 |           phone: member.user_accounts.phone_number,
ERROR in src/services/ajoNotificationService.ts:89:39
TS2339: Property 'phone_number' does not exist on type '{ phone_number: any; }[]'.
    87 |
    88 |         await ajoNotificationService.sendSMS({
  > 89 |           phone: member.user_accounts.phone_number,
       |                                       ^^^^^^^^^^^^
    90 |           message,
    91 |           type: 'contribution_due'
    92 |         });
ERROR in src/services/ajoNotificationService.ts:121:96
TS2339: Property 'name' does not exist on type '{ name: any; }[]'.
    119 |       if (!user?.phone_number) throw new Error('User phone not found');
    120 |
  > 121 |       const message = `Congratulations! Your Ajo payout of ₦${amount} from ${member.ajo_groups.name} is ready! Go to your dashboard to withdraw. E don pay o!`;
        |                                                                                                ^^^^
    122 |
    123 |       await ajoNotificationService.sendSMS({
    124 |         phone: user.phone_number,
ERROR in src/services/ajoNotificationService.ts:155:70
TS2339: Property 'name' does not exist on type '{ name: any; }[]'.
    153 |       if (!user?.phone_number) throw new Error('User phone not found');
    154 |
  > 155 |       const message = `Your Ajo contribution for ${member.ajo_groups.name} has been marked as DEFAULT. Your reliability score has been affected. Contact group admin to resolve.`;
        |                                                                      ^^^^
    156 |
    157 |       await ajoNotificationService.sendSMS({
    158 |         phone: user.phone_number,
ERROR in src/services/ajoPaymentService.ts:2:10
TS2305: Module '"../types/ajo"' has no exported member 'AjoFormData'.
    1 | import { supabase } from '../lib/supabase';
  > 2 | import { AjoFormData } from '../types/ajo';
      |          ^^^^^^^^^^^
    3 |
    4 | export interface PaymentInitRequest {
    5 |   email: string;   onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                       ^^^^^^^^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:71
TS1434: Unexpected keyword or identifier.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                       ^
ERROR in src/components/ajo/KYCBadge.tsx:1:71
TS2304: Cannot find name 'n'.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                       ^
ERROR in src/components/ajo/KYCBadge.tsx:1:82
TS2362: The left-hand side of an arithmetic operation must be of type 'any', 'number', 'bigint' or an enum type.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                  ^^^^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:94
TS2363: The right-hand side of an arithmetic operation must be of type 'any', 'number', 'bigint' or an enum type.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                              ^^^^^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:107
TS2363: The right-hand side of an arithmetic operation must be of type 'any', 'number', 'bigint' or an enum type.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                           ^^^^^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:119
TS1434: Unexpected keyword or identifier.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                       ^
ERROR in src/components/ajo/KYCBadge.tsx:1:119
TS2304: Cannot find name 'n'.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                       ^
ERROR in src/components/ajo/KYCBadge.tsx:1:122
TS2304: Cannot find name 'verifiedDate'.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                          ^^^^^^^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:135
TS1109: Expression expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                       ^
ERROR in src/components/ajo/KYCBadge.tsx:1:137
TS2693: 'string' only refers to a type, but is being used as a value here.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                         ^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:145
TS1434: Unexpected keyword or identifier.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                 ^
ERROR in src/components/ajo/KYCBadge.tsx:1:145
TS2304: Cannot find name 'n'.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                 ^
ERROR in src/components/ajo/KYCBadge.tsx:1:148
TS2304: Cannot find name 'size'.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                    ^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:153
TS1109: Expression expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                         ^
ERROR in src/components/ajo/KYCBadge.tsx:1:155
TS2362: The left-hand side of an arithmetic operation must be of type 'any', 'number', 'bigint' or an enum type.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                           ^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:162
TS2363: The right-hand side of an arithmetic operation must be of type 'any', 'number', 'bigint' or an enum type.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                  ^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:169
TS2363: The right-hand side of an arithmetic operation must be of type 'any', 'number', 'bigint' or an enum type.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                         ^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:175
TS1434: Unexpected keyword or identifier.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                               ^
ERROR in src/components/ajo/KYCBadge.tsx:1:175
TS2304: Cannot find name 'n'.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                               ^
ERROR in src/components/ajo/KYCBadge.tsx:1:178
TS2304: Cannot find name 'showTooltip'.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                  ^^^^^^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:190
TS1109: Expression expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                              ^
ERROR in src/components/ajo/KYCBadge.tsx:1:192
TS2693: 'boolean' only refers to a type, but is being used as a value here.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                ^^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:201
TS1434: Unexpected keyword or identifier.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                         ^
ERROR in src/components/ajo/KYCBadge.tsx:1:201
TS2304: Cannot find name 'n'.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                         ^
ERROR in src/components/ajo/KYCBadge.tsx:1:204
TS2304: Cannot find name 'onClick'.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                            ^^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:212
TS1109: Expression expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                    ^
ERROR in src/components/ajo/KYCBadge.tsx:1:224
TS1109: Expression expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                ^
ERROR in src/components/ajo/KYCBadge.tsx:1:226
TS2304: Cannot find name 'n'.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                  ^
ERROR in src/components/ajo/KYCBadge.tsx:1:229
TS2304: Cannot find name 'n'.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                     ^
ERROR in src/components/ajo/KYCBadge.tsx:1:231
TS1435: Unknown keyword or identifier. Did you mean 'export'?
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                       ^^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:231
TS2304: Cannot find name 'nexport'.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                       ^^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:264
TS2304: Cannot find name 'KYCBadgeProps'.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                        ^^^^^^^^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:284
TS2300: Duplicate identifier 'n'.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                            ^
ERROR in src/components/ajo/KYCBadge.tsx:1:287
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                               ^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:295
TS2300: Duplicate identifier 'n'.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                       ^
ERROR in src/components/ajo/KYCBadge.tsx:1:298
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                          ^^^^^^^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:312
TS2300: Duplicate identifier 'n'.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                        ^
ERROR in src/components/ajo/KYCBadge.tsx:1:315
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                           ^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:328
TS2300: Duplicate identifier 'n'.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                        ^
ERROR in src/components/ajo/KYCBadge.tsx:1:331
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                           ^^^^^^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:351
TS2300: Duplicate identifier 'n'.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                               ^
ERROR in src/components/ajo/KYCBadge.tsx:1:354
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                  ^^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:363
TS2300: Duplicate identifier 'n'.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                           ^
ERROR in src/components/ajo/KYCBadge.tsx:1:372
TS1434: Unexpected keyword or identifier.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                    ^
ERROR in src/components/ajo/KYCBadge.tsx:1:446
TS1434: Unexpected keyword or identifier.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                              ^
ERROR in src/components/ajo/KYCBadge.tsx:1:476
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            ^^
ERROR in src/components/ajo/KYCBadge.tsx:1:504
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        ^^
ERROR in src/components/ajo/KYCBadge.tsx:1:532
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    ^^
ERROR in src/components/ajo/KYCBadge.tsx:1:565
TS1434: Unexpected keyword or identifier.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     ^
ERROR in src/components/ajo/KYCBadge.tsx:1:596
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    ^^^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:615
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       ^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:633
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         ^^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:665
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         ^^^^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:701
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             ^^^^^^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:741
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     ^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:771
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   ^^^^^^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:831
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               ^^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:849
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 ^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:867
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   ^^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:900
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    ^^^^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:937
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         ^^^^^^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:978
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  ^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:1007
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               ^^^^^^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:1061
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     ^^^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:1080
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        ^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:1098
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          ^^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:1128
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        ^^^^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:1162
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          ^^^^^^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:1200
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                ^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:1230
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              ^^^^^^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:1301
TS1434: Unexpected keyword or identifier.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     ^
ERROR in src/components/ajo/KYCBadge.tsx:1:1343
TS1434: Unexpected keyword or identifier.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               ^
ERROR in src/components/ajo/KYCBadge.tsx:1:1361
TS2304: Cannot find name 'div'.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 ^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:1365
TS1005: ';' expected.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     ^^^^^^^^^
ERROR in src/components/ajo/KYCBadge.tsx:1:1365
TS2304: Cannot find name 'className'.
  > 1 | import React, { useState } from 'react';\n\ninterface KYCBadgeProps {\n  status: 'pending' | 'verified' | 'rejected';\n  verifiedDate?: string;\n  size?: 'sm' | 'md' | 'lg';\n  showTooltip?: boolean;\n  onClick?: () => void;\n}\n\nexport const KYCBadge: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  size = 'md',\n  showTooltip = true,\n  onClick,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const sizeClasses = {\n    sm: 'w-5 h-5 text-xs',\n    md: 'w-6 h-6 text-sm',\n    lg: 'w-8 h-8 text-base',\n  };\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      borderColor: 'border-green-300',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      borderColor: 'border-yellow-300',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      borderColor: 'border-red-300',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      {/* Badge */}\n      <div\n        className={`\n          flex items-center justify-center\n          ${sizeClasses[size]}\n          ${config.bgColor}\n          ${config.textColor}\n          border-2 ${config.borderColor}\n          rounded-full\n          font-bold\n          cursor-pointer\n          hover:shadow-md\n          transition-all\n          ${onClick ? 'hover:scale-110' : ''}\n        `}\n        onClick={onClick}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n        title={config.label}\n      >\n        {config.icon}\n      </div>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\n// Inline Badge (for text)\nexport const KYCBadgeInline: React.FC<KYCBadgeProps> = ({\n  status,\n  verifiedDate,\n  showTooltip = true,\n}) => {\n  const [showTooltipContent, setShowTooltipContent] = useState(false);\n\n  const statusConfig = {\n    verified: {\n      icon: '✓',\n      bgColor: 'bg-green-100',\n      textColor: 'text-green-700',\n      label: 'KYC Verified',\n      description: 'Identity verified and approved',\n    },\n    pending: {\n      icon: '⏳',\n      bgColor: 'bg-yellow-100',\n      textColor: 'text-yellow-700',\n      label: 'KYC Pending',\n      description: 'Verification in progress',\n    },\n    rejected: {\n      icon: '✕',\n      bgColor: 'bg-red-100',\n      textColor: 'text-red-700',\n      label: 'KYC Rejected',\n      description: 'Verification failed. Please try again.',\n    },\n  };\n\n  const config = statusConfig[status];\n\n  return (\n    <div className=\"relative inline-block\">\n      <span\n        className={`\n          inline-flex items-center gap-1\n          px-2 py-1\n          ${config.bgColor}\n          ${config.textColor}\n          rounded-full\n          text-xs font-semibold\n          cursor-help\n        `}\n        onMouseEnter={() => showTooltip && setShowTooltipContent(true)}\n        onMouseLeave={() => setShowTooltipContent(false)}\n      >\n        <span>{config.icon}</span>\n        <span>{config.label}</span>\n      </span>\n\n      {/* Tooltip */}\n      {showTooltip && showTooltipContent && (\n        <div className=\"absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 z-50\">\n          <div className=\"bg-gray-900 text-white px-3 py-2 rounded-lg shadow-lg whitespace-nowrap text-xs\">\n            <p className=\"font-semibold\">{config.label}</p>\n            <p className=\"text-gray-200\">{config.description}</p>\n            {verifiedDate && (\n              <p className=\"text-gray-300 mt-1\">\n                {new Date(verifiedDate).toLocaleDateString()}\n              </p>\n            )}\n            {/* Arrow */}\n            <div className=\"absolute top-full left-1/2 transform -translate-x-1/2 -mt-1\">\n              <div className=\"border-4 border-transparent border-t-gray-900\" />\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KYCBadge;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     ^^^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx
TS1005: '}' expected.
ERROR in src/components/ajo/MemberProfileCard.tsx
TS1127: Invalid character.
ERROR in src/components/ajo/MemberProfileCard.tsx:1:42
TS1435: Unknown keyword or identifier. Did you mean 'import'?
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                          ^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:42
TS2304: Cannot find name 'nimport'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                          ^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:52
TS2304: Cannot find name 'KYCBadge'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                    ^^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:63
TS1434: Unexpected keyword or identifier.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                               ^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:63
TS2304: Cannot find name 'from'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                               ^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:82
TS2304: Cannot find name 'n'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                  ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:84
TS1435: Unknown keyword or identifier. Did you mean 'interface'?
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                    ^^^^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:84
TS2304: Cannot find name 'ninterface'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                    ^^^^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:95
TS1434: Unexpected keyword or identifier.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                               ^^^^^^^^^^^^^^^^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:95
TS2304: Cannot find name 'MemberProfileCardProps'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                               ^^^^^^^^^^^^^^^^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:120
TS1434: Unexpected keyword or identifier.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                        ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:120
TS2304: Cannot find name 'n'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                        ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:133
TS2693: 'number' only refers to a type, but is being used as a value here.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                     ^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:141
TS1434: Unexpected keyword or identifier.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                             ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:141
TS2304: Cannot find name 'n'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                             ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:155
TS2693: 'string' only refers to a type, but is being used as a value here.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                           ^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:163
TS1434: Unexpected keyword or identifier.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                   ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:163
TS2304: Cannot find name 'n'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                   ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:176
TS2693: 'string' only refers to a type, but is being used as a value here.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                ^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:184
TS1434: Unexpected keyword or identifier.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                        ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:184
TS2304: Cannot find name 'n'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                        ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:194
TS2693: 'string' only refers to a type, but is being used as a value here.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                  ^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:202
TS1434: Unexpected keyword or identifier.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                          ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:202
TS2304: Cannot find name 'n'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                          ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:205
TS2304: Cannot find name 'photoUrl'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                             ^^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:214
TS1109: Expression expected.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                      ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:216
TS2693: 'string' only refers to a type, but is being used as a value here.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                        ^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:224
TS1434: Unexpected keyword or identifier.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:224
TS2304: Cannot find name 'n'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:238
TS2362: The left-hand side of an arithmetic operation must be of type 'any', 'number', 'bigint' or an enum type.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                              ^^^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:250
TS2363: The right-hand side of an arithmetic operation must be of type 'any', 'number', 'bigint' or an enum type.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                          ^^^^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:263
TS2363: The right-hand side of an arithmetic operation must be of type 'any', 'number', 'bigint' or an enum type.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                       ^^^^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:275
TS1434: Unexpected keyword or identifier.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                   ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:275
TS2304: Cannot find name 'n'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                   ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:278
TS2304: Cannot find name 'kycVerifiedDate'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                      ^^^^^^^^^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:294
TS1109: Expression expected.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                      ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:296
TS2693: 'string' only refers to a type, but is being used as a value here.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                        ^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:304
TS1434: Unexpected keyword or identifier.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:304
TS2304: Cannot find name 'n'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:325
TS2693: 'number' only refers to a type, but is being used as a value here.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                     ^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:333
TS1434: Unexpected keyword or identifier.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                             ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:333
TS2304: Cannot find name 'n'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                             ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:336
TS2304: Cannot find name 'guarantorCount'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                ^^^^^^^^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:351
TS1109: Expression expected.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                               ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:353
TS2693: 'number' only refers to a type, but is being used as a value here.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                 ^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:361
TS1434: Unexpected keyword or identifier.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                         ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:361
TS2304: Cannot find name 'n'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                         ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:364
TS2304: Cannot find name 'paymentStatus'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                            ^^^^^^^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:378
TS1109: Expression expected.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                          ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:380
TS2362: The left-hand side of an arithmetic operation must be of type 'any', 'number', 'bigint' or an enum type.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                            ^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:389
TS2363: The right-hand side of an arithmetic operation must be of type 'any', 'number', 'bigint' or an enum type.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                     ^^^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:401
TS2363: The right-hand side of an arithmetic operation must be of type 'any', 'number', 'bigint' or an enum type.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                 ^^^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:412
TS1434: Unexpected keyword or identifier.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                            ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:412
TS2304: Cannot find name 'n'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                            ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:415
TS2304: Cannot find name 'position'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                               ^^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:424
TS1109: Expression expected.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                        ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:426
TS2693: 'number' only refers to a type, but is being used as a value here.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                          ^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:434
TS1434: Unexpected keyword or identifier.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                  ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:434
TS2304: Cannot find name 'n'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                  ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:437
TS2304: Cannot find name 'onClick'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                     ^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:445
TS1109: Expression expected.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                             ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:457
TS1109: Expression expected.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                         ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:459
TS1434: Unexpected keyword or identifier.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                           ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:459
TS2304: Cannot find name 'n'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                           ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:462
TS2304: Cannot find name 'showDetails'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                              ^^^^^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:474
TS1109: Expression expected.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:476
TS2693: 'boolean' only refers to a type, but is being used as a value here.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            ^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:485
TS2304: Cannot find name 'n'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:488
TS2304: Cannot find name 'n'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:490
TS1435: Unknown keyword or identifier. Did you mean 'export'?
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          ^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:490
TS2304: Cannot find name 'nexport'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          ^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:504
TS2322: Type '({ n, memberId, n, firstName, n, lastName, n, email, n, photoUrl, n, kycStatus, n, kycVerifiedDate, n, reliabilityScore, n, guarantorCount, n, paymentStatus, n, position, n, onClick, n, showDetails, n }: MemberProfileCardProps) => void' is not assignable to type 'FC<MemberProfileCardProps>'.
  Type 'void' is not assignable to type 'ReactElement<any, any>'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        ^^^^^^^^^^^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:532
TS2304: Cannot find name 'MemberProfileCardProps'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    ^^^^^^^^^^^^^^^^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:561
TS2300: Duplicate identifier 'n'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:564
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    ^^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:574
TS2300: Duplicate identifier 'n'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:577
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 ^^^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:588
TS2300: Duplicate identifier 'n'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:591
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               ^^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:601
TS2300: Duplicate identifier 'n'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:604
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            ^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:611
TS2300: Duplicate identifier 'n'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:614
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      ^^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:624
TS2300: Duplicate identifier 'n'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:627
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   ^^^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:638
TS2300: Duplicate identifier 'n'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:641
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 ^^^^^^^^^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:658
TS2300: Duplicate identifier 'n'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:661
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     ^^^^^^^^^^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:679
TS2300: Duplicate identifier 'n'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:682
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          ^^^^^^^^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:702
TS2300: Duplicate identifier 'n'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:705
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 ^^^^^^^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:732
TS2300: Duplicate identifier 'n'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:735
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               ^^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:745
TS2300: Duplicate identifier 'n'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:748
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            ^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:757
TS2300: Duplicate identifier 'n'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:760
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        ^^^^^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:781
TS2300: Duplicate identifier 'n'.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:790
TS1434: Unexpected keyword or identifier.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:844
TS1434: Unexpected keyword or identifier.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:908
TS1434: Unexpected keyword or identifier.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:1039
TS1434: Unexpected keyword or identifier.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:1147
TS1434: Unexpected keyword or identifier.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           ^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:1185
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 ^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:1270
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      ^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:1363
TS1005: ',' expected.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   ^^^^^^^
ERROR in src/components/ajo/MemberProfileCard.tsx:1:1454
TS1434: Unexpected keyword or identifier.
  > 1 | import React, { useState } from 'react';\nimport { KYCBadge } from './KYCBadge';\n\ninterface MemberProfileCardProps {\n  memberId: number;\n  firstName: string;\n  lastName: string;\n  email: string;\n  photoUrl?: string;\n  kycStatus: 'pending' | 'verified' | 'rejected';\n  kycVerifiedDate?: string;\n  reliabilityScore: number;\n  guarantorCount?: number;\n  paymentStatus?: 'paid' | 'pending' | 'overdue';\n  position?: number;\n  onClick?: () => void;\n  showDetails?: boolean;\n}\n\nexport const MemberProfileCard: React.FC<MemberProfileCardProps> = ({\n  memberId,\n  firstName,\n  lastName,\n  email,\n  photoUrl,\n  kycStatus,\n  kycVerifiedDate,\n  reliabilityScore,\n  guarantorCount = 0,\n  paymentStatus = 'pending',\n  position,\n  onClick,\n  showDetails = false,\n}) => {\n  const [isHovered, setIsHovered] = useState(false);\n  const [showFullEmail, setShowFullEmail] = useState(false);\n\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n  const scoreLabel = reliabilityScore >= 0.85 ? 'Excellent' : reliabilityScore >= 0.7 ? 'Good' : 'Fair';\n\n  const paymentStatusConfig = {\n    paid: { icon: '✓', color: 'text-green-600', bg: 'bg-green-50', label: 'Paid' },\n    pending: { icon: '⏳', color: 'text-yellow-600', bg: 'bg-yellow-50', label: 'Pending' },\n    overdue: { icon: '⚠️', color: 'text-red-600', bg: 'bg-red-50', label: 'Overdue' },\n  };\n\n  const paymentConfig = paymentStatusConfig[paymentStatus];\n\n  // Mask email\n  const maskedEmail = email.split('@')[0].substring(0, 3) + '***@' + email.split('@')[1];\n\n  return (\n    <div\n      className=\"bg-white rounded-xl shadow-md p-4 border border-gray-200 hover:shadow-lg transition-all cursor-pointer\"\n      onMouseEnter={() => setIsHovered(true)}\n      onMouseLeave={() => setIsHovered(false)}\n      onClick={onClick}\n    >\n      {/* Header with Position */}\n      {position && (\n        <div className=\"absolute top-2 right-2 bg-primary-green text-white rounded-full w-8 h-8 flex items-center justify-center text-sm font-bold\">\n          {position}\n        </div>\n      )}\n\n      {/* Photo */}\n      <div className=\"mb-3 flex justify-center\">\n        {photoUrl ? (\n          <img\n            src={photoUrl}\n            alt={`${firstName} ${lastName}`}\n            className=\"w-16 h-16 rounded-full object-cover border-2 border-primary-green shadow-md\"\n          />\n        ) : (\n          <div className=\"w-16 h-16 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-2xl font-bold shadow-md\">\n            {firstName.charAt(0)}{lastName.charAt(0)}\n          </div>\n        )}\n      </div>\n\n      {/* Name */}\n      <h3 className=\"text-center font-bold text-gray-900 text-sm truncate\">\n        {firstName} {lastName}\n      </h3>\n\n      {/* Email */}\n      <p\n        className=\"text-center text-xs text-gray-600 truncate cursor-help\"\n        onMouseEnter={() => setShowFullEmail(true)}\n        onMouseLeave={() => setShowFullEmail(false)}\n        title={email}\n      >\n        {showFullEmail ? email : maskedEmail}\n      </p>\n\n      {/* KYC Badge */}\n      <div className=\"flex justify-center mt-2 mb-3\">\n        <KYCBadge\n          status={kycStatus}\n          verifiedDate={kycVerifiedDate}\n          size=\"sm\"\n          showTooltip={true}\n        />\n      </div>\n\n      {/* Reliability Score */}\n      <div className=\"mb-3 p-2 bg-gray-50 rounded-lg\">\n        <div className=\"flex items-center justify-between mb-1\">\n          <span className=\"text-xs font-semibold text-gray-700\">Trust Score</span>\n          <span className={`text-xs font-bold ${scoreColor}`}>\n            {(reliabilityScore * 100).toFixed(0)}%\n          </span>\n        </div>\n        <div className=\"w-full bg-gray-300 rounded-full h-1.5\">\n          <div\n            className={`h-1.5 rounded-full transition-all ${\n              reliabilityScore >= 0.85\n                ? 'bg-green-500'\n                : reliabilityScore >= 0.7\n                ? 'bg-blue-500'\n                : 'bg-yellow-500'\n            }`}\n            style={{ width: `${reliabilityScore * 100}%` }}\n          />\n        </div>\n        <p className={`text-xs mt-1 font-semibold ${scoreColor}`}>{scoreLabel}</p>\n      </div>\n\n      {/* Payment Status */}\n      <div className={`flex items-center justify-center gap-1 p-2 rounded-lg ${paymentConfig.bg} mb-3`}>\n        <span className={paymentConfig.color}>{paymentConfig.icon}</span>\n        <span className={`text-xs font-semibold ${paymentConfig.color}`}>\n          {paymentConfig.label}\n        </span>\n      </div>\n\n      {/* Guarantor Badge */}\n      {guarantorCount > 0 && (\n        <div className=\"flex items-center justify-center gap-1 p-2 bg-blue-50 rounded-lg mb-3\">\n          <span className=\"text-blue-600\">🤝</span>\n          <span className=\"text-xs font-semibold text-blue-600\">\n            {guarantorCount} Guarantor{guarantorCount !== 1 ? 's' : ''}\n          </span>\n        </div>\n      )}\n\n      {/* Details Button */}\n      {showDetails && (\n        <button className=\"w-full px-3 py-2 bg-primary-green text-white rounded-lg hover:bg-green-600 transition-colors text-xs font-semibold\">\n          View Profile\n        </button>\n      )}\n\n      {/* Hover Overlay */}\n      {isHovered && (\n        <div className=\"absolute inset-0 bg-black/5 rounded-xl pointer-events-none\" />\n      )}\n    </div>\n  );\n};\n\n// Compact version for tables\nexport const MemberProfileCompact: React.FC<Omit<MemberProfileCardProps, 'showDetails'>> = ({\n  firstName,\n  lastName,\n  photoUrl,\n  kycStatus,\n  reliabilityScore,\n  paymentStatus,\n  onClick,\n}) => {\n  const scoreColor = reliabilityScore >= 0.85 ? 'text-green-600' : reliabilityScore >= 0.7 ? 'text-blue-600' : 'text-yellow-600';\n\n  return (\n    <div\n      className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer transition-colors\"\n      onClick={onClick}\n    >\n      {/* Photo */}\n      {photoUrl ? (\n        <img\n          src={photoUrl}\n          alt={`${firstName} ${lastName}`}\n          className=\"w-10 h-10 rounded-full object-cover border border-primary-green\"\n        />\n      ) : (\n        <div className=\"w-10 h-10 rounded-full bg-gradient-to-br from-primary-green to-green-600 flex items-center justify-center text-white text-sm font-bold\">\n          {firstName.charAt(0)}{lastName.charAt(0)}\n        </div>\n      )}\n\n      {/* Name & KYC */}\n      <div className=\"flex-1 min-w-0\">\n        <p className=\"text-sm font-semibold text-gray-900 truncate\">\n          {firstName} {lastName}\n        </p>\n        <div className=\"flex items-center gap-1 mt-0.5\">\n          <KYCBadge status={kycStatus} size=\"sm\" showTooltip={false} />\n        </div>\n      </div>\n\n      {/* Score */}\n      <div className=\"text-right\">\n        <p className={`text-sm font-bold ${scoreColor}`}>\n          {(reliabilityScore * 100).toFixed(0)}%\n        </p>\n        <p className=\"text-xs text-gray-500\">\n          {paymentStatus === 'paid' ? '✓' : paymentStatus === 'overdue' ? '⚠️' : '⏳'}\n        </p>\n      </div>\n    </div>\n  );\n};\n\nexport default MemberProfileCard;\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              ^
ERROR in src/components/ajo/PersonalAjoForm.tsx:2:31
TS2307: Cannot find module '../contexts/OnboardingContext' or its corresponding type declarations.
    1 | import React from 'react';
  > 2 | import { useOnboarding } from '../contexts/OnboardingContext';
      |                               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    3 | import { calculateTotalContribution, formatCurrency, getFrequencyLabel } from '../utils/ajoFormValidation';
    4 |
    5 | export const PersonalAjoForm: React.FC = () => {
ERROR in src/components/ajo/PersonalAjoForm.tsx:3:79
TS2307: Cannot find module '../utils/ajoFormValidation' or its corresponding type declarations.
    1 | import React from 'react';
    2 | import { useOnboarding } from '../contexts/OnboardingContext';
  > 3 | import { calculateTotalContribution, formatCurrency, getFrequencyLabel } from '../utils/ajoFormValidation';
      |                                                                               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    4 |
    5 | export const PersonalAjoForm: React.FC = () => {
    6 |   const { formData, errors, updateFormData, goBack, goNext } = useOnboarding();
ERROR in src/hooks/useAjoRealtime.ts:2:26
TS2307: Cannot find module 'react-query' or its corresponding type declarations.
    1 | import { useEffect, useState, useCallback } from 'react';
  > 2 | import { useQuery } from 'react-query';
      |                          ^^^^^^^^^^^^^
    3 | import { supabase } from '../lib/supabase';
    4 |
    5 | export const useAjoGroups = (userId: number) => {
ERROR in src/hooks/useAjoRealtime.ts:114:8
TS2339: Property 'on' does not exist on type 'SupabaseClient<any, "public", "public", any, any>'.
    112 |   useEffect(() => {
    113 |     const subscription = supabase
  > 114 |       .on('postgres_changes', { event: '*', schema: 'public', table }, callback)
        |        ^^
    115 |       .subscribe();
    116 |
    117 |     return () => {
ERROR in src/pages/AjoAdmin.tsx:2:26
TS2307: Cannot find module 'react-query' or its corresponding type declarations.
    1 | import React, { useState } from 'react';
  > 2 | import { useQuery } from 'react-query';
      |                          ^^^^^^^^^^^^^
    3 | import { supabase } from '../lib/supabase';
    4 | import { UserLayout } from '../components/UserLayout';
    5 | import { LoadingSpinner } from '../components/LoadingSpinner';
ERROR in src/pages/AjoAdmin.tsx:4:10
TS2614: Module '"../components/UserLayout"' has no exported member 'UserLayout'. Did you mean to use 'import UserLayout from "../components/UserLayout"' instead?
    2 | import { useQuery } from 'react-query';
    3 | import { supabase } from '../lib/supabase';
  > 4 | import { UserLayout } from '../components/UserLayout';
      |          ^^^^^^^^^^
    5 | import { LoadingSpinner } from '../components/LoadingSpinner';
    6 | import { Alert } from '../components/Alert';
    7 |
ERROR in src/pages/AjoAdmin.tsx:5:10
TS2614: Module '"../components/LoadingSpinner"' has no exported member 'LoadingSpinner'. Did you mean to use 'import LoadingSpinner from "../components/LoadingSpinner"' instead?
    3 | import { supabase } from '../lib/supabase';
    4 | import { UserLayout } from '../components/UserLayout';
  > 5 | import { LoadingSpinner } from '../components/LoadingSpinner';
      |          ^^^^^^^^^^^^^^
    6 | import { Alert } from '../components/Alert';
    7 |
    8 | export default function AjoAdmin() {
ERROR in src/pages/AjoAdmin.tsx:6:10
TS2614: Module '"../components/Alert"' has no exported member 'Alert'. Did you mean to use 'import Alert from "../components/Alert"' instead?
    4 | import { UserLayout } from '../components/UserLayout';
    5 | import { LoadingSpinner } from '../components/LoadingSpinner';
  > 6 | import { Alert } from '../components/Alert';
      |          ^^^^^
    7 |
    8 | export default function AjoAdmin() {
    9 |   const [activeTab, setActiveTab] = useState<'groups' | 'cycles' | 'members'>('groups');
ERROR in src/pages/AjoCheckout.tsx:7:10
TS2614: Module '"../components/Alert"' has no exported member 'Alert'. Did you mean to use 'import Alert from "../components/Alert"' instead?
     5 | import { ajoPaymentService } from '../services/ajoPaymentService';
     6 | import { formatCurrency, getFrequencyLabel } from '../utils/ajoFormValidation';
  >  7 | import { Alert } from '../components/Alert';
       |          ^^^^^
     8 |
     9 | export const AjoCheckout: React.FC = () => {
    10 |   const navigate = useNavigate();
ERROR in src/pages/AjoCheckout.tsx:12:21
TS2339: Property 'setIsSubmitting' does not exist on type 'OnboardingContextType'.
    10 |   const navigate = useNavigate();
    11 |   const { user } = useAuth();
  > 12 |   const { formData, setIsSubmitting, isSubmitting } = useOnboarding();
       |                     ^^^^^^^^^^^^^^^
    13 |   const [error, setError] = useState('');
    14 |   const [isLoading, setIsLoading] = useState(false);
    15 |
ERROR in src/pages/AjoCheckout.tsx:12:38
TS2339: Property 'isSubmitting' does not exist on type 'OnboardingContextType'.
    10 |   const navigate = useNavigate();
    11 |   const { user } = useAuth();
  > 12 |   const { formData, setIsSubmitting, isSubmitting } = useOnboarding();
       |                                      ^^^^^^^^^^^^
    13 |   const [error, setError] = useState('');
    14 |   const [isLoading, setIsLoading] = useState(false);
    15 |
ERROR in src/pages/AjoCheckout.tsx:31:33
TS2339: Property 'full_name' does not exist on type 'User'.
    29 |     try {
    30 |       // Get user's full name and phone from profile
  > 31 |       const userFullName = user.full_name || user.email.split('@')[0];
       |                                 ^^^^^^^^^
    32 |       const userPhone = user.phone_number || '+234';
    33 |
    34 |       // Initialize payment
ERROR in src/pages/AjoCheckout.tsx:32:30
TS2339: Property 'phone_number' does not exist on type 'User'.
    30 |       // Get user's full name and phone from profile
    31 |       const userFullName = user.full_name || user.email.split('@')[0];
  > 32 |       const userPhone = user.phone_number || '+234';
       |                              ^^^^^^^^^^^^
    33 |
    34 |       // Initialize payment
    35 |       const paymentInit = await ajoPaymentService.initializePayment({
ERROR in src/pages/AjoCheckout.tsx:40:9
TS2322: Type 'string' is not assignable to type 'number'.
    38 |         ajoType: formData.type,
    39 |         ajoData: formData,
  > 40 |         userId: user.id,
       |         ^^^^^^
    41 |         fullName: userFullName,
    42 |         phoneNumber: userPhone,
    43 |       });
ERROR in src/pages/AjoContribute.tsx:6:10
TS2614: Module '"../components/Alert"' has no exported member 'Alert'. Did you mean to use 'import Alert from "../components/Alert"' instead?
    4 | import { contributionPaymentService } from '../services/contributionPaymentService';
    5 | import { formatCurrency, getFrequencyLabel } from '../utils/ajoFormValidation';
  > 6 | import { Alert } from '../components/Alert';
      |          ^^^^^
    7 |
    8 | interface PendingContribution {
    9 |   id: number;
ERROR in src/pages/AjoContribute.tsx:43:86
TS2345: Argument of type 'string' is not assignable to parameter of type 'number'.
    41 |     try {
    42 |       setIsLoading(true);
  > 43 |       const contributions = await contributionPaymentService.getPendingContributions(user.id);
       |                                                                                      ^^^^^^^
    44 |       setPendingContributions(contributions);
    45 |     } catch (err: any) {
    46 |       setError(err.message || 'Failed to fetch pending contributions');
ERROR in src/pages/AjoContribute.tsx:63:9
TS2322: Type 'string' is not assignable to type 'number'.
    61 |     try {
    62 |       const paymentInit = await contributionPaymentService.initializeContributionPayment({
  > 63 |         userId: user.id,
       |         ^^^^^^
    64 |         groupId: contribution.group_id,
    65 |         cycleId: contribution.cycle_id,
    66 |         amount: contribution.amount_due,
ERROR in src/pages/AjoContribute.tsx:68:24
TS2339: Property 'full_name' does not exist on type 'User'.
    66 |         amount: contribution.amount_due,
    67 |         email: user.email,
  > 68 |         fullName: user.full_name || user.email.split('@')[0],
       |                        ^^^^^^^^^
    69 |         phoneNumber: user.phone_number || '+234',
    70 |       });
    71 |
ERROR in src/pages/AjoContribute.tsx:69:27
TS2339: Property 'phone_number' does not exist on type 'User'.
    67 |         email: user.email,
    68 |         fullName: user.full_name || user.email.split('@')[0],
  > 69 |         phoneNumber: user.phone_number || '+234',
       |                           ^^^^^^^^^^^^
    70 |       });
    71 |
    72 |       // Redirect to Flutterwave
ERROR in src/pages/AjoDashboard.tsx:2:26
TS2307: Cannot find module 'react-query' or its corresponding type declarations.
    1 | import React, { useEffect, useState } from 'react';
  > 2 | import { useQuery } from 'react-query';
      |                          ^^^^^^^^^^^^^
    3 | import { supabase } from '../lib/supabase';
    4 | import { ajoGroupService } from '../services/ajoGroupService';
    5 | import { ajoService } from '../services/ajoService';
ERROR in src/pages/AjoDashboard.tsx:6:10
TS2614: Module '"../components/UserLayout"' has no exported member 'UserLayout'. Did you mean to use 'import UserLayout from "../components/UserLayout"' instead?
    4 | import { ajoGroupService } from '../services/ajoGroupService';
    5 | import { ajoService } from '../services/ajoService';
  > 6 | import { UserLayout } from '../components/UserLayout';
      |          ^^^^^^^^^^
    7 | import { LoadingSpinner } from '../components/LoadingSpinner';
    8 | import { Alert } from '../components/Alert';
    9 |
ERROR in src/pages/AjoDashboard.tsx:7:10
TS2614: Module '"../components/LoadingSpinner"' has no exported member 'LoadingSpinner'. Did you mean to use 'import LoadingSpinner from "../components/LoadingSpinner"' instead?
     5 | import { ajoService } from '../services/ajoService';
     6 | import { UserLayout } from '../components/UserLayout';
  >  7 | import { LoadingSpinner } from '../components/LoadingSpinner';
       |          ^^^^^^^^^^^^^^
     8 | import { Alert } from '../components/Alert';
     9 |
    10 | interface DashboardStats {
ERROR in src/pages/AjoDashboard.tsx:8:10
TS2614: Module '"../components/Alert"' has no exported member 'Alert'. Did you mean to use 'import Alert from "../components/Alert"' instead?
     6 | import { UserLayout } from '../components/UserLayout';
     7 | import { LoadingSpinner } from '../components/LoadingSpinner';
  >  8 | import { Alert } from '../components/Alert';
       |          ^^^^^
     9 |
    10 | interface DashboardStats {
    11 |   activeGroups: number;
ERROR in src/pages/AjoDashboardEnhanced.tsx:10:64
TS2345: Argument of type 'string | 0' is not assignable to parameter of type 'number'.
  Type 'string' is not assignable to type 'number'.
     8 |   const [activeTab, setActiveTab] = useState<'overview' | 'groups' | 'transactions' | 'alerts'>('overview');
     9 |   
  > 10 |   const { data: stats, isLoading: statsLoading } = useAjoStats(user?.id || 0);
       |                                                                ^^^^^^^^^^^^^
    11 |   const { data: groups, isLoading: groupsLoading } = useAjoGroups(user?.id || 0);
    12 |   const { data: transactions, isLoading: txLoading } = useAjoTransactions(user?.id || 0);
    13 |   const { data: graceNotices, isLoading: noticesLoading } = useAjoGraceNotices(user?.id || 0);
ERROR in src/pages/AjoDashboardEnhanced.tsx:11:67
TS2345: Argument of type 'string | 0' is not assignable to parameter of type 'number'.
  Type 'string' is not assignable to type 'number'.
     9 |   
    10 |   const { data: stats, isLoading: statsLoading } = useAjoStats(user?.id || 0);
  > 11 |   const { data: groups, isLoading: groupsLoading } = useAjoGroups(user?.id || 0);
       |                                                                   ^^^^^^^^^^^^^
    12 |   const { data: transactions, isLoading: txLoading } = useAjoTransactions(user?.id || 0);
    13 |   const { data: graceNotices, isLoading: noticesLoading } = useAjoGraceNotices(user?.id || 0);
    14 |
ERROR in src/pages/AjoDashboardEnhanced.tsx:12:75
TS2345: Argument of type 'string | 0' is not assignable to parameter of type 'number'.
  Type 'string' is not assignable to type 'number'.
    10 |   const { data: stats, isLoading: statsLoading } = useAjoStats(user?.id || 0);
    11 |   const { data: groups, isLoading: groupsLoading } = useAjoGroups(user?.id || 0);
  > 12 |   const { data: transactions, isLoading: txLoading } = useAjoTransactions(user?.id || 0);
       |                                                                           ^^^^^^^^^^^^^
    13 |   const { data: graceNotices, isLoading: noticesLoading } = useAjoGraceNotices(user?.id || 0);
    14 |
    15 |   const chartData = transactions?.slice(0, 7).reverse().map((tx, idx) => ({
ERROR in src/pages/AjoDashboardEnhanced.tsx:13:80
TS2345: Argument of type 'string | 0' is not assignable to parameter of type 'number'.
  Type 'string' is not assignable to type 'number'.
    11 |   const { data: groups, isLoading: groupsLoading } = useAjoGroups(user?.id || 0);
    12 |   const { data: transactions, isLoading: txLoading } = useAjoTransactions(user?.id || 0);
  > 13 |   const { data: graceNotices, isLoading: noticesLoading } = useAjoGraceNotices(user?.id || 0);
       |                                                                                ^^^^^^^^^^^^^
    14 |
    15 |   const chartData = transactions?.slice(0, 7).reverse().map((tx, idx) => ({
    16 |     name: `Day ${idx + 1}`,
ERROR in src/pages/AjoGroupDetail.tsx:3:26
TS2307: Cannot find module 'react-query' or its corresponding type declarations.
    1 | import React, { useState } from 'react';
    2 | import { useParams } from 'react-router-dom';
  > 3 | import { useQuery } from 'react-query';
      |                          ^^^^^^^^^^^^^
    4 | import { ajoGroupService } from '../services/ajoGroupService';
    5 | import { UserLayout } from '../components/UserLayout';
    6 | import { LoadingSpinner } from '../components/LoadingSpinner';
ERROR in src/pages/AjoGroupDetail.tsx:5:10
TS2614: Module '"../components/UserLayout"' has no exported member 'UserLayout'. Did you mean to use 'import UserLayout from "../components/UserLayout"' instead?
    3 | import { useQuery } from 'react-query';
    4 | import { ajoGroupService } from '../services/ajoGroupService';
  > 5 | import { UserLayout } from '../components/UserLayout';
      |          ^^^^^^^^^^
    6 | import { LoadingSpinner } from '../components/LoadingSpinner';
    7 | import { Alert } from '../components/Alert';
    8 |
ERROR in src/pages/AjoGroupDetail.tsx:6:10
TS2614: Module '"../components/LoadingSpinner"' has no exported member 'LoadingSpinner'. Did you mean to use 'import LoadingSpinner from "../components/LoadingSpinner"' instead?
    4 | import { ajoGroupService } from '../services/ajoGroupService';
    5 | import { UserLayout } from '../components/UserLayout';
  > 6 | import { LoadingSpinner } from '../components/LoadingSpinner';
      |          ^^^^^^^^^^^^^^
    7 | import { Alert } from '../components/Alert';
    8 |
    9 | export default function AjoGroupDetail() {
ERROR in src/pages/AjoGroupDetail.tsx:7:10
TS2614: Module '"../components/Alert"' has no exported member 'Alert'. Did you mean to use 'import Alert from "../components/Alert"' instead?
     5 | import { UserLayout } from '../components/UserLayout';
     6 | import { LoadingSpinner } from '../components/LoadingSpinner';
  >  7 | import { Alert } from '../components/Alert';
       |          ^^^^^
     8 |
     9 | export default function AjoGroupDetail() {
    10 |   const { groupId } = useParams<{ groupId: string }>();
ERROR in src/pages/AjoGroups.tsx
TS1005: '}' expected.
ERROR in src/pages/AjoGroups.tsx:2:26
TS2307: Cannot find module 'react-query' or its corresponding type declarations.
    1 | import React, { useState, useEffect } from 'react';
  > 2 | import { useQuery } from 'react-query';
      |                          ^^^^^^^^^^^^^
    3 | import { useAuth } from '../contexts/AuthContext';
    4 | import { ajoGroupBrowseService, GroupBrowseFilter } from '../services/ajoGroupBrowseService';
    5 | import { ajoJoinRequestService } from '../services/ajoJoinRequestService';
ERROR in src/pages/AjoGroups.tsx:6:10
TS2614: Module '"../components/UserLayout"' has no exported member 'UserLayout'. Did you mean to use 'import UserLayout from "../components/UserLayout"' instead?
    4 | import { ajoGroupBrowseService, GroupBrowseFilter } from '../services/ajoGroupBrowseService';
    5 | import { ajoJoinRequestService } from '../services/ajoJoinRequestService';
  > 6 | import { UserLayout } from '../components/UserLayout';
      |          ^^^^^^^^^^
    7 | import { LoadingSpinner } from '../components/LoadingSpinner';
    8 | import { Alert } from '../components/Alert';
    9 | import { GroupBrowseCard } from '../components/ajo/GroupBrowseCard';
ERROR in src/pages/AjoGroups.tsx:7:10
TS2614: Module '"../components/LoadingSpinner"' has no exported member 'LoadingSpinner'. Did you mean to use 'import LoadingSpinner from "../components/LoadingSpinner"' instead?
     5 | import { ajoJoinRequestService } from '../services/ajoJoinRequestService';
     6 | import { UserLayout } from '../components/UserLayout';
  >  7 | import { LoadingSpinner } from '../components/LoadingSpinner';
       |          ^^^^^^^^^^^^^^
     8 | import { Alert } from '../components/Alert';
     9 | import { GroupBrowseCard } from '../components/ajo/GroupBrowseCard';
    10 | import { JoinRequestModal } from '../components/ajo/JoinRequestModal';
ERROR in src/pages/AjoGroups.tsx:8:10
TS2614: Module '"../components/Alert"' has no exported member 'Alert'. Did you mean to use 'import Alert from "../components/Alert"' instead?
     6 | import { UserLayout } from '../components/UserLayout';
     7 | import { LoadingSpinner } from '../components/LoadingSpinner';
  >  8 | import { Alert } from '../components/Alert';
       |          ^^^^^
     9 | import { GroupBrowseCard } from '../components/ajo/GroupBrowseCard';
    10 | import { JoinRequestModal } from '../components/ajo/JoinRequestModal';
    11 |
ERROR in src/pages/AjoGroups.tsx:45:52
TS2339: Property 'getUserJoinRequests' does not exist on type '{ submitJoinRequest(userId: number, groupId: number): Promise<{ data: any; error: any; } | { data: any; error: any; }>; approveJoinRequest(requestId: number, adminId: number): Promise<...>; ... 5 more ...; subscribeToJoinRequests(groupId: number, callback: (request: any) => void): RealtimeChannel; }'.
    43 |     async () => {
    44 |       if (!user?.id) return [];
  > 45 |       const { data } = await ajoJoinRequestService.getUserJoinRequests(user.id);
       |                                                    ^^^^^^^^^^^^^^^^^^^
    46 |       return data || [];
    47 |     },
    48 |     { enabled: !!user?.id }
ERROR in src/pages/AjoGroups.tsx:345:11
TS2322: Type 'string | number' is not assignable to type 'number'.
  Type 'string' is not assignable to type 'number'.
    343 |           groupId={selectedGroup.id}
    344 |           groupName={selectedGroup.name}
  > 345 |           userId={user?.id || 0}
        |           ^^^^^^
    346 |           onClose={() => {
    347 |             setShowJoinModal(false);
    348 |             setSelectedGroup(null);
ERROR in src/pages/AjoOnboarding.tsx:4:10
TS2614: Module '"../components/UserLayout"' has no exported member 'UserLayout'. Did you mean to use 'import UserLayout from "../components/UserLayout"' instead?
    2 | import { useNavigate } from 'react-router-dom';
    3 | import { ajoService } from '../services/ajoService';
  > 4 | import { UserLayout } from '../components/UserLayout';
      |          ^^^^^^^^^^
    5 | import { Alert } from '../components/Alert';
    6 |
    7 | type AjoType = 'personal' | 'group';
ERROR in src/pages/AjoOnboarding.tsx:5:10
TS2614: Module '"../components/Alert"' has no exported member 'Alert'. Did you mean to use 'import Alert from "../components/Alert"' instead?
    3 | import { ajoService } from '../services/ajoService';
    4 | import { UserLayout } from '../components/UserLayout';
  > 5 | import { Alert } from '../components/Alert';
      |          ^^^^^
    6 |
    7 | type AjoType = 'personal' | 'group';
    8 |
ERROR in src/pages/AjoOnboarding.tsx:47:49
TS2345: Argument of type 'FormData' is not assignable to parameter of type 'CreateAjoData'.
  Type 'FormData' is missing the following properties from type 'CreateAjoData': paymentReference, firstPayment
    45 |     setError('');
    46 |     try {
  > 47 |       const result = await ajoService.createAjo(formData);
       |                                                 ^^^^^^^^
    48 |       navigate('/ajo/dashboard?success=created');
    49 |     } catch (err: any) {
    50 |       setError(err.message || 'Failed to create Ajo');
ERROR in src/pages/AjoPaymentCallback.tsx:10:21
TS2339: Property 'resetForm' does not exist on type 'OnboardingContextType'.
     8 |   const navigate = useNavigate();
     9 |   const { user } = useAuth();
  > 10 |   const { formData, resetForm } = useOnboarding();
       |                     ^^^^^^^^^
    11 |   const [searchParams] = useSearchParams();
    12 |   const [isProcessing, setIsProcessing] = useState(true);
    13 |
ERROR in src/pages/AjoPaymentCallback.tsx:40:17
TS2345: Argument of type 'string' is not assignable to parameter of type 'number'.
    38 |             try {
    39 |               await ajoPaymentService.createAjoAfterPayment(
  > 40 |                 user.id,
       |                 ^^^^^^^
    41 |                 formData,
    42 |                 verification.data.tx_ref
    43 |               );
ERROR in src/pages/AjoProfile.tsx:2:26
TS2307: Cannot find module 'react-query' or its corresponding type declarations.
    1 | import React from 'react';
  > 2 | import { useQuery } from 'react-query';
      |                          ^^^^^^^^^^^^^
    3 | import { supabase } from '../lib/supabase';
    4 | import { UserLayout } from '../components/UserLayout';
    5 | import { LoadingSpinner } from '../components/LoadingSpinner';
ERROR in src/pages/AjoProfile.tsx:4:10
TS2614: Module '"../components/UserLayout"' has no exported member 'UserLayout'. Did you mean to use 'import UserLayout from "../components/UserLayout"' instead?
    2 | import { useQuery } from 'react-query';
    3 | import { supabase } from '../lib/supabase';
  > 4 | import { UserLayout } from '../components/UserLayout';
      |          ^^^^^^^^^^
    5 | import { LoadingSpinner } from '../components/LoadingSpinner';
    6 | import { Alert } from '../components/Alert';
    7 |
ERROR in src/pages/AjoProfile.tsx:5:10
TS2614: Module '"../components/LoadingSpinner"' has no exported member 'LoadingSpinner'. Did you mean to use 'import LoadingSpinner from "../components/LoadingSpinner"' instead?
    3 | import { supabase } from '../lib/supabase';
    4 | import { UserLayout } from '../components/UserLayout';
  > 5 | import { LoadingSpinner } from '../components/LoadingSpinner';
      |          ^^^^^^^^^^^^^^
    6 | import { Alert } from '../components/Alert';
    7 |
    8 | export default function AjoProfile() {
ERROR in src/pages/AjoProfile.tsx:6:10
TS2614: Module '"../components/Alert"' has no exported member 'Alert'. Did you mean to use 'import Alert from "../components/Alert"' instead?
    4 | import { UserLayout } from '../components/UserLayout';
    5 | import { LoadingSpinner } from '../components/LoadingSpinner';
  > 6 | import { Alert } from '../components/Alert';
      |          ^^^^^
    7 |
    8 | export default function AjoProfile() {
    9 |   // Fetch user profile
ERROR in src/pages/AjoWithdraw.tsx:3:26
TS2307: Cannot find module 'react-query' or its corresponding type declarations.
    1 | import React, { useState, useEffect } from 'react';
    2 | import { useParams, useNavigate } from 'react-router-dom';
  > 3 | import { useQuery } from 'react-query';
      |                          ^^^^^^^^^^^^^
    4 | import { ajoGroupService } from '../services/ajoGroupService';
    5 | import { ajoPositionService } from '../services/ajoPositionService';
    6 | import { UserLayout } from '../components/UserLayout';
ERROR in src/pages/AjoWithdraw.tsx:6:10
TS2614: Module '"../components/UserLayout"' has no exported member 'UserLayout'. Did you mean to use 'import UserLayout from "../components/UserLayout"' instead?
    4 | import { ajoGroupService } from '../services/ajoGroupService';
    5 | import { ajoPositionService } from '../services/ajoPositionService';
  > 6 | import { UserLayout } from '../components/UserLayout';
      |          ^^^^^^^^^^
    7 | import { LoadingSpinner } from '../components/LoadingSpinner';
    8 | import { Alert } from '../components/Alert';
    9 |
ERROR in src/pages/AjoWithdraw.tsx:7:10
TS2614: Module '"../components/LoadingSpinner"' has no exported member 'LoadingSpinner'. Did you mean to use 'import LoadingSpinner from "../components/LoadingSpinner"' instead?
     5 | import { ajoPositionService } from '../services/ajoPositionService';
     6 | import { UserLayout } from '../components/UserLayout';
  >  7 | import { LoadingSpinner } from '../components/LoadingSpinner';
       |          ^^^^^^^^^^^^^^
     8 | import { Alert } from '../components/Alert';
     9 |
    10 | interface WithdrawalState {
ERROR in src/pages/AjoWithdraw.tsx:8:10
TS2614: Module '"../components/Alert"' has no exported member 'Alert'. Did you mean to use 'import Alert from "../components/Alert"' instead?
     6 | import { UserLayout } from '../components/UserLayout';
     7 | import { LoadingSpinner } from '../components/LoadingSpinner';
  >  8 | import { Alert } from '../components/Alert';
       |          ^^^^^
     9 |
    10 | interface WithdrawalState {
    11 |   withdrawalType: 'full' | 'partial';
ERROR in src/pages/AjoWithdraw.tsx:45:43
TS2304: Cannot find name 'supabase'.
    43 |     async () => {
    44 |       if (!cycle) return null;
  > 45 |       const { data: { session } } = await supabase.auth.getSession();
       |                                           ^^^^^^^^
    46 |       if (!session) return null;
    47 |       
    48 |       const { data: userData } = await supabase
ERROR in src/pages/AjoWithdraw.tsx:48:40
TS2304: Cannot find name 'supabase'.
    46 |       if (!session) return null;
    47 |       
  > 48 |       const { data: userData } = await supabase
       |                                        ^^^^^^^^
    49 |         .from('user_accounts')
    50 |         .select('Id')
    51 |         .eq('Email', session.user.email)
ERROR in src/services/ajoJoinRequestService.ts:63:10
TS2551: Property 'catch' does not exist on type 'PostgrestFilterBuilder<any, any, any, any, "log_ajo_event", null, "RPC">'. Did you mean 'match'?
    61 |         event: 'join_request_submitted',
    62 |         data: { user_id: userId, group_id: groupId },
  > 63 |       }).catch(() => {});
       |          ^^^^^
    64 |
    65 |       return { data, error: null };
    66 |     } catch (error) {
ERROR in src/services/ajoJoinRequestService.ts:128:10
TS2551: Property 'catch' does not exist on type 'PostgrestFilterBuilder<any, any, any, any, "increment_group_members", null, "RPC">'. Did you mean 'match'?
    126 |       await supabase.rpc('increment_group_members', {
    127 |         p_group_id: request.group_id,
  > 128 |       }).catch(() => {});
        |          ^^^^^
    129 |
    130 |       // Send notification
    131 |       await supabase.rpc('notify_user', {
ERROR in src/services/ajoJoinRequestService.ts:136:10
TS2551: Property 'catch' does not exist on type 'PostgrestFilterBuilder<any, any, any, any, "notify_user", null, "RPC">'. Did you mean 'match'?
    134 |         p_message: 'Your request to join the group has been approved!',
    135 |         p_type: 'ajo_join_approved',
  > 136 |       }).catch(() => {});
        |          ^^^^^
    137 |
    138 |       // Log event
    139 |       await supabase.rpc('log_ajo_event', {
ERROR in src/services/ajoJoinRequestService.ts:142:10
TS2551: Property 'catch' does not exist on type 'PostgrestFilterBuilder<any, any, any, any, "log_ajo_event", null, "RPC">'. Did you mean 'match'?
    140 |         event: 'join_request_approved',
    141 |         data: { request_id: requestId, user_id: request.user_id, group_id: request.group_id },
  > 142 |       }).catch(() => {});
        |          ^^^^^
    143 |
    144 |       return { data: updated, error: null };
    145 |     } catch (error) {
ERROR in src/services/ajoJoinRequestService.ts:186:10
TS2551: Property 'catch' does not exist on type 'PostgrestFilterBuilder<any, any, any, any, "notify_user", null, "RPC">'. Did you mean 'match'?
    184 |         p_message: reason || 'Your request to join the group was not approved.',
    185 |         p_type: 'ajo_join_rejected',
  > 186 |       }).catch(() => {});
        |          ^^^^^
    187 |
    188 |       // Log event
    189 |       await supabase.rpc('log_ajo_event', {
ERROR in src/services/ajoJoinRequestService.ts:192:10
TS2551: Property 'catch' does not exist on type 'PostgrestFilterBuilder<any, any, any, any, "log_ajo_event", null, "RPC">'. Did you mean 'match'?
    190 |         event: 'join_request_rejected',
    191 |         data: { request_id: requestId, user_id: request.user_id, group_id: request.group_id, reason },
  > 192 |       }).catch(() => {});
        |          ^^^^^
    193 |
    194 |       return { data: updated, error: null };
    195 |     } catch (error) {
ERROR in src/services/ajoJoinRequestService.ts:228:10
TS2551: Property 'catch' does not exist on type 'PostgrestFilterBuilder<any, any, any, any, "log_ajo_event", null, "RPC">'. Did you mean 'match'?
    226 |         event: 'join_request_cancelled',
    227 |         data: { request_id: requestId, user_id: userId, group_id: request.group_id },
  > 228 |       }).catch(() => {});
        |          ^^^^^
    229 |
    230 |       return { data: { success: true }, error: null };
    231 |     } catch (error) {
ERROR in src/services/ajoMemberProfileService.ts
TS1127: Invalid character.
ERROR in src/services/ajoMemberProfileService.ts:1:45
TS2304: Cannot find name 'n'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                             ^
ERROR in src/services/ajoMemberProfileService.ts:1:47
TS1435: Unknown keyword or identifier. Did you mean 'export'?
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                               ^^^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:47
TS2304: Cannot find name 'nexport'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                               ^^^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:81
TS1131: Property or signature expected.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                 ^
ERROR in src/services/ajoMemberProfileService.ts:1:81
TS2304: Cannot find name 'n'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                 ^
ERROR in src/services/ajoMemberProfileService.ts:1:88
TS2693: 'number' only refers to a type, but is being used as a value here.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                        ^^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:96
TS1434: Unexpected keyword or identifier.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                ^
ERROR in src/services/ajoMemberProfileService.ts:1:96
TS2304: Cannot find name 'n'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                ^
ERROR in src/services/ajoMemberProfileService.ts:1:108
TS2693: 'number' only refers to a type, but is being used as a value here.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                            ^^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:116
TS1434: Unexpected keyword or identifier.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                    ^
ERROR in src/services/ajoMemberProfileService.ts:1:116
TS2304: Cannot find name 'n'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                    ^
ERROR in src/services/ajoMemberProfileService.ts:1:119
TS2304: Cannot find name 'photo_url'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                       ^^^^^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:129
TS1109: Expression expected.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                 ^
ERROR in src/services/ajoMemberProfileService.ts:1:131
TS2693: 'string' only refers to a type, but is being used as a value here.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                   ^^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:139
TS1434: Unexpected keyword or identifier.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                           ^
ERROR in src/services/ajoMemberProfileService.ts:1:139
TS2304: Cannot find name 'n'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                           ^
ERROR in src/services/ajoMemberProfileService.ts:1:154
TS2362: The left-hand side of an arithmetic operation must be of type 'any', 'number', 'bigint' or an enum type.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                          ^^^^^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:166
TS2363: The right-hand side of an arithmetic operation must be of type 'any', 'number', 'bigint' or an enum type.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                      ^^^^^^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:179
TS2363: The right-hand side of an arithmetic operation must be of type 'any', 'number', 'bigint' or an enum type.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                   ^^^^^^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:191
TS1434: Unexpected keyword or identifier.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                               ^
ERROR in src/services/ajoMemberProfileService.ts:1:191
TS2304: Cannot find name 'n'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                               ^
ERROR in src/services/ajoMemberProfileService.ts:1:194
TS2304: Cannot find name 'kyc_verified_date'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                  ^^^^^^^^^^^^^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:212
TS1109: Expression expected.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                    ^
ERROR in src/services/ajoMemberProfileService.ts:1:214
TS2693: 'string' only refers to a type, but is being used as a value here.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                      ^^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:222
TS1434: Unexpected keyword or identifier.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                              ^
ERROR in src/services/ajoMemberProfileService.ts:1:222
TS2304: Cannot find name 'n'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                              ^
ERROR in src/services/ajoMemberProfileService.ts:1:225
TS2304: Cannot find name 'bio'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                 ^^^
ERROR in src/services/ajoMemberProfileService.ts:1:229
TS1109: Expression expected.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                     ^
ERROR in src/services/ajoMemberProfileService.ts:1:231
TS2693: 'string' only refers to a type, but is being used as a value here.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                       ^^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:239
TS1434: Unexpected keyword or identifier.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                               ^
ERROR in src/services/ajoMemberProfileService.ts:1:239
TS2304: Cannot find name 'n'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                               ^
ERROR in src/services/ajoMemberProfileService.ts:1:258
TS2693: 'boolean' only refers to a type, but is being used as a value here.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                  ^^^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:267
TS1434: Unexpected keyword or identifier.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                           ^
ERROR in src/services/ajoMemberProfileService.ts:1:267
TS2304: Cannot find name 'n'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                           ^
ERROR in src/services/ajoMemberProfileService.ts:1:282
TS2693: 'string' only refers to a type, but is being used as a value here.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                          ^^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:290
TS1434: Unexpected keyword or identifier.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                  ^
ERROR in src/services/ajoMemberProfileService.ts:1:290
TS2304: Cannot find name 'n'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                  ^
ERROR in src/services/ajoMemberProfileService.ts:1:305
TS2693: 'string' only refers to a type, but is being used as a value here.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                 ^^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:313
TS2304: Cannot find name 'n'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                         ^
ERROR in src/services/ajoMemberProfileService.ts:1:314
TS1128: Declaration or statement expected.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                          ^
ERROR in src/services/ajoMemberProfileService.ts:1:316
TS2304: Cannot find name 'n'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                            ^
ERROR in src/services/ajoMemberProfileService.ts:1:318
TS1435: Unknown keyword or identifier. Did you mean 'export'?
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                              ^^^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:318
TS2304: Cannot find name 'nexport'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                              ^^^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:350
TS1131: Property or signature expected.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                              ^
ERROR in src/services/ajoMemberProfileService.ts:1:350
TS2304: Cannot find name 'n'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                              ^
ERROR in src/services/ajoMemberProfileService.ts:1:367
TS2693: 'number' only refers to a type, but is being used as a value here.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                               ^^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:375
TS1434: Unexpected keyword or identifier.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                       ^
ERROR in src/services/ajoMemberProfileService.ts:1:375
TS2304: Cannot find name 'n'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                       ^
ERROR in src/services/ajoMemberProfileService.ts:1:397
TS2693: 'number' only refers to a type, but is being used as a value here.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                             ^^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:405
TS1434: Unexpected keyword or identifier.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                     ^
ERROR in src/services/ajoMemberProfileService.ts:1:405
TS2304: Cannot find name 'n'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                     ^
ERROR in src/services/ajoMemberProfileService.ts:1:424
TS2693: 'number' only refers to a type, but is being used as a value here.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                        ^^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:432
TS1434: Unexpected keyword or identifier.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                ^
ERROR in src/services/ajoMemberProfileService.ts:1:432
TS2304: Cannot find name 'n'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                ^
ERROR in src/services/ajoMemberProfileService.ts:1:449
TS2693: 'number' only refers to a type, but is being used as a value here.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                 ^^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:457
TS1434: Unexpected keyword or identifier.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                         ^
ERROR in src/services/ajoMemberProfileService.ts:1:457
TS2304: Cannot find name 'n'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                         ^
ERROR in src/services/ajoMemberProfileService.ts:1:479
TS2693: 'number' only refers to a type, but is being used as a value here.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               ^^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:487
TS1434: Unexpected keyword or identifier.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       ^
ERROR in src/services/ajoMemberProfileService.ts:1:487
TS2304: Cannot find name 'n'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       ^
ERROR in src/services/ajoMemberProfileService.ts:1:507
TS2693: 'number' only refers to a type, but is being used as a value here.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           ^^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:515
TS1434: Unexpected keyword or identifier.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   ^
ERROR in src/services/ajoMemberProfileService.ts:1:515
TS2304: Cannot find name 'n'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   ^
ERROR in src/services/ajoMemberProfileService.ts:1:537
TS2693: 'number' only refers to a type, but is being used as a value here.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         ^^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:545
TS2304: Cannot find name 'n'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 ^
ERROR in src/services/ajoMemberProfileService.ts:1:546
TS1128: Declaration or statement expected.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  ^
ERROR in src/services/ajoMemberProfileService.ts:1:548
TS2304: Cannot find name 'n'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    ^
ERROR in src/services/ajoMemberProfileService.ts:1:550
TS1435: Unknown keyword or identifier. Did you mean 'export'?
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      ^^^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:550
TS2304: Cannot find name 'nexport'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      ^^^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:592
TS18004: No value exists in scope for the shorthand property 'n'. Either declare one or provide an initializer.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                ^
ERROR in src/services/ajoMemberProfileService.ts:1:631
TS18004: No value exists in scope for the shorthand property 'n'. Either declare one or provide an initializer.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       ^
ERROR in src/services/ajoMemberProfileService.ts:1:634
TS1005: ',' expected.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          ^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:675
TS1434: Unexpected keyword or identifier.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   ^
ERROR in src/services/ajoMemberProfileService.ts:1:675
TS2304: Cannot find name 'n'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   ^
ERROR in src/services/ajoMemberProfileService.ts:1:686
TS1434: Unexpected keyword or identifier.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              ^
ERROR in src/services/ajoMemberProfileService.ts:1:686
TS2448: Block-scoped variable 'n' used before its declaration.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              ^
ERROR in src/services/ajoMemberProfileService.ts:1:701
TS2339: Property 'data' does not exist on type 'SupabaseClient<any, "public", "public", any, any>'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             ^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:707
TS2339: Property 'error' does not exist on type 'SupabaseClient<any, "public", "public", any, any>'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   ^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:741
TS1005: ',' expected.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     ^
ERROR in src/services/ajoMemberProfileService.ts:1:770
TS18004: No value exists in scope for the shorthand property 'n'. Either declare one or provide an initializer.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  ^
ERROR in src/services/ajoMemberProfileService.ts:1:779
TS1005: ',' expected.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           ^
ERROR in src/services/ajoMemberProfileService.ts:1:792
TS18004: No value exists in scope for the shorthand property 'n'. Either declare one or provide an initializer.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        ^
ERROR in src/services/ajoMemberProfileService.ts:1:801
TS1005: ',' expected.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 ^
ERROR in src/services/ajoMemberProfileService.ts:1:816
TS2304: Cannot find name 'userId'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                ^^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:824
TS18004: No value exists in scope for the shorthand property 'n'. Either declare one or provide an initializer.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        ^
ERROR in src/services/ajoMemberProfileService.ts:1:833
TS1005: ',' expected.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 ^
ERROR in src/services/ajoMemberProfileService.ts:1:842
TS1005: ',' expected.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          ^
ERROR in src/services/ajoMemberProfileService.ts:1:844
TS18004: No value exists in scope for the shorthand property 'n'. Either declare one or provide an initializer.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            ^
ERROR in src/services/ajoMemberProfileService.ts:1:846
TS18004: No value exists in scope for the shorthand property 'n'. Either declare one or provide an initializer.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              ^
ERROR in src/services/ajoMemberProfileService.ts:1:853
TS1005: ',' expected.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     ^^
ERROR in src/services/ajoMemberProfileService.ts:1:863
TS1005: ',' expected.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               ^^
ERROR in src/services/ajoMemberProfileService.ts:1:866
TS2304: Cannot find name 'error'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  ^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:891
TS1005: ',' expected.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           ^
ERROR in src/services/ajoMemberProfileService.ts:1:893
TS1389: 'throw' is not allowed as a variable declaration name.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             ^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:899
TS2304: Cannot find name 'error'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   ^^^^^
ERROR in src/services/ajoMemberProfileService.ts:1:906
TS2304: Cannot find name 'n'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          ^
ERROR in src/services/ajoMemberProfileService.ts:1:908
TS2304: Cannot find name 'n'.
  > 1 | import { supabase } from '../lib/supabase';\n\nexport interface MemberProfile {\n  id: number;\n  user_id: number;\n  photo_url?: string;\n  kyc_status: 'pending' | 'verified' | 'rejected';\n  kyc_verified_date?: string;\n  bio?: string;\n  phone_verified: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport interface MemberStats {\n  total_groups: number;\n  total_contributed: number;\n  total_received: number;\n  on_time_rate: number;\n  reliability_score: number;\n  guarantor_count: number;\n  collateral_amount: number;\n}\n\nexport const ajoMemberProfileService = {\n  /**\n   * Get member profile\n   */\n  async getMemberProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      // If no profile exists, create default\n      if (!data) {\n        return await this.createDefaultProfile(userId);\n      }\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error getting member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Create default profile for new user\n   */\n  async createDefaultProfile(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .insert({\n          user_id: userId,\n          kyc_status: 'pending',\n          phone_verified: false,\n          created_at: new Date().toISOString(),\n          updated_at: new Date().toISOString(),\n        })\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error creating default profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Update member profile\n   */\n  async updateMemberProfile(userId: number, updates: Partial<MemberProfile>) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          ...updates,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_profile_updated',\n        data: { user_id: userId, updates },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error updating member profile:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Upload member photo\n   */\n  async uploadMemberPhoto(userId: number, file: File) {\n    try {\n      // Validate file\n      if (!file.type.startsWith('image/')) {\n        throw new Error('File must be an image');\n      }\n\n      if (file.size > 5 * 1024 * 1024) {\n        throw new Error('File size must be less than 5MB');\n      }\n\n      // Upload to storage\n      const fileName = `member-${userId}-${Date.now()}.jpg`;\n      const { data: uploadData, error: uploadError } = await supabase.storage\n        .from('ajo-member-photos')\n        .upload(fileName, file, {\n          cacheControl: '3600',\n          upsert: false,\n        });\n\n      if (uploadError) throw uploadError;\n\n      // Get public URL\n      const { data: urlData } = supabase.storage\n        .from('ajo-member-photos')\n        .getPublicUrl(fileName);\n\n      // Update profile with photo URL\n      const { data: profile, error: updateError } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: urlData.publicUrl,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (updateError) throw updateError;\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'member_photo_uploaded',\n        data: { user_id: userId, file_name: fileName },\n      }).catch(() => {});\n\n      return { data: profile, error: null };\n    } catch (error) {\n      console.error('Error uploading member photo:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get KYC status\n   */\n  async getKYCStatus(userId: number) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .select('kyc_status, kyc_verified_date')\n        .eq('user_id', userId)\n        .single();\n\n      if (error && error.code !== 'PGRST116') throw error;\n\n      return {\n        data: {\n          status: data?.kyc_status || 'pending',\n          verified_date: data?.kyc_verified_date,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting KYC status:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Verify KYC (admin only)\n   */\n  async verifyKYC(userId: number, adminId: number, status: 'verified' | 'rejected') {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          kyc_status: status,\n          kyc_verified_date: status === 'verified' ? new Date().toISOString() : null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // Send notification\n      const message = status === 'verified'\n        ? 'Your KYC verification has been approved!'\n        : 'Your KYC verification was rejected. Please try again.';\n\n      await supabase.rpc('notify_user', {\n        p_user_id: userId,\n        p_title: 'KYC Verification',\n        p_message: message,\n        p_type: `kyc_${status}`,\n      }).catch(() => {});\n\n      // Log event\n      await supabase.rpc('log_ajo_event', {\n        event: 'kyc_verified',\n        data: { user_id: userId, status, admin_id: adminId },\n      }).catch(() => {});\n\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error verifying KYC:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member statistics\n   */\n  async getMemberStats(userId: number): Promise<{ data: MemberStats | null; error: any }> {\n    try {\n      // Get groups\n      const { data: memberships } = await supabase\n        .from('ajo_memberships')\n        .select('group_id')\n        .eq('user_id', userId);\n\n      const groupCount = memberships?.length || 0;\n\n      // Get contributions\n      const { data: contributions } = await supabase\n        .from('ajo_contributions')\n        .select('amount, status, due_date, paid_date')\n        .eq('user_id', userId);\n\n      const totalContributed = contributions?.reduce((sum, c) => sum + (c.amount || 0), 0) || 0;\n      const onTimeCount = contributions?.filter(c => {\n        if (!c.due_date || !c.paid_date) return false;\n        return new Date(c.paid_date) <= new Date(c.due_date);\n      }).length || 0;\n      const onTimeRate = contributions?.length ? (onTimeCount / contributions.length) * 100 : 0;\n\n      // Get payouts\n      const { data: payouts } = await supabase\n        .from('ajo_payouts')\n        .select('amount')\n        .eq('user_id', userId);\n\n      const totalReceived = payouts?.reduce((sum, p) => sum + (p.amount || 0), 0) || 0;\n\n      // Get reliability score\n      const { data: scoreData } = await supabase\n        .from('ajo_member_scores')\n        .select('score')\n        .eq('user_id', userId)\n        .single();\n\n      const reliabilityScore = scoreData?.score || 0.5;\n\n      // Get guarantor count\n      const { data: guarantors } = await supabase\n        .from('ajo_guarantors')\n        .select('id')\n        .eq('member_id', userId);\n\n      const guarantorCount = guarantors?.length || 0;\n\n      // Get collateral\n      const { data: collateral } = await supabase\n        .from('ajo_collateral')\n        .select('amount')\n        .eq('member_id', userId)\n        .eq('status', 'active')\n        .single();\n\n      const collateralAmount = collateral?.amount || 0;\n\n      return {\n        data: {\n          total_groups: groupCount,\n          total_contributed: totalContributed,\n          total_received: totalReceived,\n          on_time_rate: onTimeRate,\n          reliability_score: reliabilityScore,\n          guarantor_count: guarantorCount,\n          collateral_amount: collateralAmount,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member stats:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get member contribution history\n   */\n  async getMemberHistory(userId: number, limit = 20) {\n    try {\n      const { data, error } = await supabase\n        .from('ajo_contributions')\n        .select(`\n          id, amount, status, due_date, paid_date,\n          ajo_cycles(cycle_number),\n          ajo_groups(name)\n        `)\n        .eq('user_id', userId)\n        .order('created_at', { ascending: false })\n        .limit(limit);\n\n      if (error) throw error;\n      return { data: data || [], error: null };\n    } catch (error) {\n      console.error('Error getting member history:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Get member by ID (for display)\n   */\n  async getMemberById(userId: number) {\n    try {\n      const { data: profile, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .eq('user_id', userId)\n        .single();\n\n      if (profileError && profileError.code !== 'PGRST116') throw profileError;\n\n      const { data: user, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .eq('Id', userId)\n        .single();\n\n      if (userError) throw userError;\n\n      return {\n        data: {\n          ...profile,\n          user: user,\n        },\n        error: null,\n      };\n    } catch (error) {\n      console.error('Error getting member by ID:', error);\n      return { data: null, error };\n    }\n  },\n\n  /**\n   * Get multiple members (for group display)\n   */\n  async getMembers(userIds: number[]) {\n    try {\n      const { data: profiles, error: profileError } = await supabase\n        .from('ajo_member_profiles')\n        .select('*')\n        .in('user_id', userIds);\n\n      if (profileError) throw profileError;\n\n      const { data: users, error: userError } = await supabase\n        .from('user_accounts')\n        .select('Id, FirstName, LastName, Email, phone_number')\n        .in('Id', userIds);\n\n      if (userError) throw userError;\n\n      // Merge data\n      const members = userIds.map(id => ({\n        profile: profiles?.find(p => p.user_id === id),\n        user: users?.find(u => u.Id === id),\n      }));\n\n      return { data: members, error: null };\n    } catch (error) {\n      console.error('Error getting members:', error);\n      return { data: [], error };\n    }\n  },\n\n  /**\n   * Subscribe to member profile updates\n   */\n  subscribeToMemberUpdates(userId: number, callback: (profile: any) => void) {\n    const subscription = supabase\n      .channel(`member-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_member_profiles',\n          filter: `user_id=eq.${userId}`,\n        },\n        (payload) => {\n          callback(payload.new);\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Subscribe to member stats updates\n   */\n  subscribeToMemberStats(userId: number, callback: (stats: any) => void) {\n    const subscription = supabase\n      .channel(`member-stats-${userId}`)\n      .on(\n        'postgres_changes',\n        {\n          event: '*',\n          schema: 'public',\n          table: 'ajo_contributions',\n          filter: `user_id=eq.${userId}`,\n        },\n        () => {\n          // Recalculate stats\n          this.getMemberStats(userId).then(result => {\n            if (result.data) callback(result.data);\n          });\n        }\n      )\n      .subscribe();\n\n    return subscription;\n  },\n\n  /**\n   * Delete member photo\n   */\n  async deleteMemberPhoto(userId: number) {\n    try {\n      // Get current photo URL\n      const { data: profile } = await supabase\n        .from('ajo_member_profiles')\n        .select('photo_url')\n        .eq('user_id', userId)\n        .single();\n\n      if (profile?.photo_url) {\n        // Extract file name from URL\n        const fileName = profile.photo_url.split('/').pop();\n        if (fileName) {\n          await supabase.storage\n            .from('ajo-member-photos')\n            .remove([fileName]);\n        }\n      }\n\n      // Update profile\n      const { data, error } = await supabase\n        .from('ajo_member_profiles')\n        .update({\n          photo_url: null,\n          updated_at: new Date().toISOString(),\n        })\n        .eq('user_id', userId)\n        .select()\n        .single();\n\n      if (error) throw error;\n      return { data, error: null };\n    } catch (error) {\n      console.error('Error deleting member photo:', error);\n      return { data: null, error };\n    }\n  },\n};\n
      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            ^
ERROR in src/services/ajoNotificationService.ts:33:69
TS2339: Property 'name' does not exist on type '{ name: any; }[]'.
    31 |       // Pidgin message
    32 |       const pidginMessages = {
  > 33 |         3: `Bros/Sis, your Ajo contribution for ${member.ajo_groups.name} don miss deadline. You get 3 days grace period to pay sharp sharp! No wahala, just settle am quick quick.`,
       |                                                                     ^^^^
    34 |         2: `Oga/Madam! Last 2 days to settle your Ajo contribution for ${member.ajo_groups.name}. E don reach final warning o! Pay now before e turn to default.`,
    35 |         1: `URGENT! Just 1 day left to pay your Ajo contribution for ${member.ajo_groups.name}. After today, na default! Abeg pay now now!`
    36 |       };
ERROR in src/services/ajoNotificationService.ts:34:92
TS2339: Property 'name' does not exist on type '{ name: any; }[]'.
    32 |       const pidginMessages = {
    33 |         3: `Bros/Sis, your Ajo contribution for ${member.ajo_groups.name} don miss deadline. You get 3 days grace period to pay sharp sharp! No wahala, just settle am quick quick.`,
  > 34 |         2: `Oga/Madam! Last 2 days to settle your Ajo contribution for ${member.ajo_groups.name}. E don reach final warning o! Pay now before e turn to default.`,
       |                                                                                            ^^^^
    35 |         1: `URGENT! Just 1 day left to pay your Ajo contribution for ${member.ajo_groups.name}. After today, na default! Abeg pay now now!`
    36 |       };
    37 |
ERROR in src/services/ajoNotificationService.ts:35:90
TS2339: Property 'name' does not exist on type '{ name: any; }[]'.
    33 |         3: `Bros/Sis, your Ajo contribution for ${member.ajo_groups.name} don miss deadline. You get 3 days grace period to pay sharp sharp! No wahala, just settle am quick quick.`,
    34 |         2: `Oga/Madam! Last 2 days to settle your Ajo contribution for ${member.ajo_groups.name}. E don reach final warning o! Pay now before e turn to default.`,
  > 35 |         1: `URGENT! Just 1 day left to pay your Ajo contribution for ${member.ajo_groups.name}. After today, na default! Abeg pay now now!`
       |                                                                                          ^^^^
    36 |       };
    37 |
    38 |       const message = pidginMessages[daysRemaining as keyof typeof pidginMessages] || 
ERROR in src/services/ajoNotificationService.ts:84:36
TS2339: Property 'phone_number' does not exist on type '{ phone_number: any; }[]'.
    82 |       // Send to each member
    83 |       for (const member of members) {
  > 84 |         if (!member.user_accounts?.phone_number) continue;
       |                                    ^^^^^^^^^^^^
    85 |
    86 |         const message = `Reminder: Your Ajo contribution of ₦${cycle.ajo_groups.contribution_amount} for ${cycle.ajo_groups.name} is due by ${new Date(cycle.contribution_deadline).toLocaleDateString()}. Pay now to maintain your reliability score!`;
    87 |
ERROR in src/services/ajoNotificationService.ts:86:81
TS2339: Property 'contribution_amount' does not exist on type '{ name: any; contribution_amount: any; }[]'.
    84 |         if (!member.user_accounts?.phone_number) continue;
    85 |
  > 86 |         const message = `Reminder: Your Ajo contribution of ₦${cycle.ajo_groups.contribution_amount} for ${cycle.ajo_groups.name} is due by ${new Date(cycle.contribution_deadline).toLocaleDateString()}. Pay now to maintain your reliability score!`;
       |                                                                                 ^^^^^^^^^^^^^^^^^^^
    87 |
    88 |         await ajoNotificationService.sendSMS({
    89 |           phone: member.user_accounts.phone_number,
ERROR in src/services/ajoNotificationService.ts:86:125
TS2339: Property 'name' does not exist on type '{ name: any; contribution_amount: any; }[]'.
    84 |         if (!member.user_accounts?.phone_number) continue;
    85 |
  > 86 |         const message = `Reminder: Your Ajo contribution of ₦${cycle.ajo_groups.contribution_amount} for ${cycle.ajo_groups.name} is due by ${new Date(cycle.contribution_deadline).toLocaleDateString()}. Pay now to maintain your reliability score!`;
       |                                                                                                                             ^^^^
    87 |
    88 |         await ajoNotificationService.sendSMS({
    89 |           phone: member.user_accounts.phone_number,
ERROR in src/services/ajoNotificationService.ts:89:39
TS2339: Property 'phone_number' does not exist on type '{ phone_number: any; }[]'.
    87 |
    88 |         await ajoNotificationService.sendSMS({
  > 89 |           phone: member.user_accounts.phone_number,
       |                                       ^^^^^^^^^^^^
    90 |           message,
    91 |           type: 'contribution_due'
    92 |         });
ERROR in src/services/ajoNotificationService.ts:121:96
TS2339: Property 'name' does not exist on type '{ name: any; }[]'.
    119 |       if (!user?.phone_number) throw new Error('User phone not found');
    120 |
  > 121 |       const message = `Congratulations! Your Ajo payout of ₦${amount} from ${member.ajo_groups.name} is ready! Go to your dashboard to withdraw. E don pay o!`;
        |                                                                                                ^^^^
    122 |
    123 |       await ajoNotificationService.sendSMS({
    124 |         phone: user.phone_number,
ERROR in src/services/ajoNotificationService.ts:155:70
TS2339: Property 'name' does not exist on type '{ name: any; }[]'.
    153 |       if (!user?.phone_number) throw new Error('User phone not found');
    154 |
  > 155 |       const message = `Your Ajo contribution for ${member.ajo_groups.name} has been marked as DEFAULT. Your reliability score has been affected. Contact group admin to resolve.`;
        |                                                                      ^^^^
    156 |
    157 |       await ajoNotificationService.sendSMS({
    158 |         phone: user.phone_number,
ERROR in src/services/ajoPaymentService.ts:2:10
TS2305: Module '"../types/ajo"' has no exported member 'AjoFormData'.
    1 | import { supabase } from '../lib/supabase';
  > 2 | import { AjoFormData } from '../types/ajo';
      |          ^^^^^^^^^^^
    3 |
    4 | export interface PaymentInitRequest {
    5 |   email: string;