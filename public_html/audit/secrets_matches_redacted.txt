Redacted secret matches - generated 2026-02-02

High-priority discoveries (values redacted):

- Administration/mysql.inc.php (line ~4)
  DEFINE('DB_PASSWORD', getenv('DB_PASSWORD') ?: '[REDACTED]');
  -- Original repo contained a plaintext fallback; rotate DB password and remove fallback.

- includes/auth/reg.data.config.php (line ~163)
  $mail->Password = getenv('SMTP_PASS') ?: '[REDACTED]';
  -- SMTP credentials observed in repo prior to env migration; rotate + remove plaintext fallbacks.

- public_html/mailer.php (line ~23)
  $mail->Password = '[REDACTED]';
  -- SMTP password present; rotate immediately.

- includes/view/verification.php (line ~5)
  new Yabacon\Paystack('sk_live_[REDACTED]');
  -- Paystack live secret key present in PHP file; rotate and remove from repo.

- includes/user/withdrawal.php (multiple lines)
  "Authorization: Bearer [REDACTED]"
  -- API bearer tokens found (Paystack); rotate and move to server-only config.

- start/.env (line ~28)
  DB_PASSWORD="[REDACTED]"
  -- .env present inside `start/`; ensure this file is removed from commits and secret rotated.

- includes/view/forgot_password.php (line ~137)
  $mail->Password = '[REDACTED]';

Vendor / examples (informational)
- vendor/phpmailer/* and vendor/yabacon/paystack-php/* contain example/test files with placeholder secrets (e.g., 'secret', 'dummyclientsecret', 'dummyrefreshtoken'). These are normal in vendor/test examples but should not be mixed with real credentials.

Notes & next actions:
- Rotate all exposed credentials immediately: DB, SMTP, Paystack live/test keys, any API tokens.
- After rotation: remove plaintext secrets from repository and from any local fallbacks; consider rewriting history (git-filter-repo or BFG) if repo public.
- Complete batched per-folder scans (done for public_html, Administration, includes, assets, vendor). Some matches were vendor tests and expected; focus remediation on repo app files listed above.

If you want, I will now:
- (A) Commit a redacted report into `audit/secrets_matches_redacted.txt` (done),
- (B) Create a CSP header include and a CSRF helper JS file and add example usage patches (next).